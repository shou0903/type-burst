# DECISIONS

仕様書(TYPE_BURST_Product_Game_Technical_Spec_v1.0.md)からの決定・変更・補完の記録。
新しい決定は末尾に追記する。

---

## D-001: Vertical Slice は BLITZ のみ

- **日付:** 2026-07-20
- **内容:** 実装順序(§30)に従い、30秒 BLITZ を先に完成させた。DUEL / PRIVATE ROOM / GHOST は未実装。
- **理由:** 仕様書 §0-4「まず30秒ソロ版を完成させる」。

## D-002: game-core の公開 API はクラスベース

- **内容:** §21 の `GameCore` インターフェース(純関数)ではなく、`BlitzGame` クラスを公開 API とした。内部は Seed + 呼び出し列に対して決定論的で、`Math.random()` は使用しない。
- **理由:** ソロ版では権威サーバーとの状態共有が不要で、クラスの方がイベント発行と統合しやすい。Phase 5(Online Duel)で純関数 API へ再編する。盤面操作(`findGroup` / `applyGravity` / `findAutoGroups`)は純関数として実装済みで、再編時にそのまま流用できる。

## D-003: Tier のモーラ帯域を実データに合わせて調整

- **内容:** short 4〜7 / standard 8〜12 / long 13〜26 モーラとした(仕様書 §7.2 は short 5〜9 / standard 10〜17 / long 18〜26)。
- **理由:** 仕様書付属のサンプルデータ(20件)の tier 付けが §7.2 の帯域と一致しない(例: 「電車が駅に到着した」=13モーラが long)。サンプルの意図を優先し、全データが検証を通る帯域に再定義した。`moraCount` は手書きせず `countMora()` で読み仮名から自動計算する。

## D-004: BLITZ のトップアウトは即時終了

- **内容:** 行上昇でブロックが盤面最上段(10行)を超えたら、その時点でゲーム終了(OVERFLOW 表示)。スコアは有効。
- **理由:** 仕様書は BLITZ のトップアウトを定義していない(§5.2 の危険ライン敗北は DUEL の条件)。30秒の緊張感を保ちつつ、稼いだスコアを無効にしない。

## D-005: KPM の分母は実プレイ経過時間

- **内容:** `KPM = correctKeyCount / 実プレイ秒数 × 60`。連鎖演出中もプレイ時間に含める。
- **理由:** §18 の `activeTypingSeconds` の定義が曖昧なため、寿司打等の慣例に合わせて全プレイ時間とした。将来、盤面選択停止時間を別指標として記録する。

## D-006: 入力ロックは全ての消去解決中に適用

- **内容:** 連鎖中だけでなく、単独消去の解決中(ヒットストップ〜落下、約340ms)も新規入力を受け付けない。
- **理由:** §10.5「連鎖中は新しい入力を受け付けない」を消去解決全体に一般化。落下中の盤面へ入力すると候補選択が不安定になるため。

## D-007: 描画は Canvas 2D 直描画

- **内容:** Phaser / PixiJS を使わず、Canvas 2D API で直接描画する。
- **理由:** §21 は「PhaserまたはPixiJS相当の2D描画」。6×10ブロック+パーティクル程度なら Canvas 2D で60fpsを維持でき、依存とバンドルサイズを大幅に削減できる。ゲームロジックは React コンポーネントにも Canvas にも置かず `game-core` に隔離済み。

## D-008: 効果音は Web Audio シンセ生成

- **内容:** 音声ファイルを使わず、オシレータとノイズバッファで効果音を合成する。
- **理由:** アセット制作前にゲームフィールを検証する(Starter Prompt「初期段階では仮の四角形と簡単な効果音で構いません」)。連鎖ごとの音程上昇(§15)はスケール配列で実装。

## D-009: 長音「ー」は「-」キーで入力

- **内容:** 読み仮名に含まれる「ー」(ノート、ペース等)はハイフンキー1打で入力する。
- **理由:** サンプルデータに「のーと」「ぺーす」が含まれる。一般的なローマ字入力の慣例に従う。

## D-010: 妨害ブロックはデータ・ロジックのみ実装

- **内容:** 妨害文章(8件)、無属性ブロックの隣接破壊、攻撃力計算式は game-core に実装済みだが、BLITZ では妨害ブロックが出現しない。
- **理由:** §12-13 は DUEL の要素。Phase 4(Ghost)で疑似妨害として利用する。

## D-011: 結果画面の「苦手傾向」はヒューリスティック

- **内容:** 苦手かな・ローマ字遷移の統計(§18)は未実装。結果画面には正確率・連鎖数などから選ぶ定型アドバイス1件を表示する。
- **理由:** Vertical Slice の範囲外。Phase 6 でキー単位のミス統計を実装する。

## D-012: BLITZ を廃止しサバイバル制へ変更(v2)

- **日付:** 2026-07-20
- **内容:** 「30秒は短すぎる。時間を終了条件にしないでほしい」というプレイテストのフィードバックを受け、時間切れを廃止。行上昇が経過時間とともに加速し(5.5秒→最短2.4秒)、盤面があふれるまで続くサバイバル制にした。仕様書 §5.1 の BLITZ(30秒)とは異なる。
- **理由:** ユーザーフィードバック最優先。KPI「もう一戦率」はサバイバルの生存時間更新動機で担保する。

## D-013: 特殊ブロックと TYPE BURST の追加(v2)

- **内容:** 仕様書 §35 は「スキル」をMVP外としているが、フィードバック「必殺技や特殊ブロックが欲しい」を受けて以下を追加した。
  - **ボム**(出現率7%): 入力完了で周囲3×3を爆破
  - **プリズム**(出現率3.5%): 入力完了で盤面最多属性を全消し
  - **TYPE BURST**: 消去と連鎖でゲージが溜まり、満タン時に Enter で下3行を吹き飛ばす。バースト起因の消去ではゲージは再充填されない(無限ループ防止)
- **理由:** キャラクター・RPG要素ではなく盤面ギミックの範囲に留め、§0-9「爆発・落下・連鎖の気持ちよさ優先」に沿う形で採用。

## D-014: CPU対戦(DUEL の先行実装)

- **内容:** オンライン対戦(Phase 5)の前に、同一 game-core を使う CPU 対戦を実装した。CPU は結果を書き換えず実際にキーを1打ずつ入力する(kpm・ミス率・思考時間は難易度3段階の Config)。妨害送信・相殺・着弾(§12-13)はこのモードで稼働。時間制限はなく、相手の盤面をあふれさせたら勝ち。
- **理由:** 初期過疎対策(§5.4 GHOST と同目的)+ 対戦の面白さを早期検証。CPU も Seeded PRNG 駆動なので試合全体が決定論的でリプレイ可能。

## D-015: 対戦の勝敗判定は トップアウトのみ

- **内容:** §5.2 の「危険ライン超え2秒維持で勝利」は未実装。盤面最上段を超えた側の負けのみ。
- **理由:** 時間制限を廃止したため時間切れ判定が不要になり、ルールを1つに絞った方が分かりやすい。2秒ルールはオンライン版で再検討する。

## D-016: 行のせり上がりを「上からの落下」へ変更(v3)

- **日付:** 2026-07-20
- **内容:** 仕様書 §11 の「下から1行追加し全体を押し上げる」を廃止し、新しい行は盤面の上から降ってきて各列の山の一番上に積まれる方式にした。既存ブロックは一切動かない。
- **理由:** フィードバック「上から降ってくるほうが分かりやすいし演出的にもいい」。副次効果として、入力中のブロックが行追加で動かなくなり、タイピングの継続性も向上した。

## D-017: 選択キャンセル操作の追加(v3)

- **内容:** Esc または Backspace で入力中・ロック中の選択を解除できる。キャンセルはミスに数えず、正確率にも影響しない。
- **理由:** フィードバック「入力を開始したブロックをキャンセルしたい」。§9.3「バックスペースを必須にしない」とは矛盾しない(必須ではなく任意の救済手段)。

## D-018: ALL CLEAR ボーナス・LEVEL・ランク評価(v3)

- **内容:** 楽しさを増す進行システムを追加した。
  - **ALL CLEAR**: 盤面を空にすると +5000点・ゲージ+50。次の行が600ms以内に降ってくる
  - **LEVEL**(サバイバル): 30秒生存ごとにレベルアップ(+レベル×300点)。行上昇の加速と合わせて進行感を可視化
  - **ランク評価**(サバイバル結果): スコアで S+/S/A/B/C/D を表示
  - HUD に PERFECT ストリーク(🔥×n)を表示
- **理由:** フィードバック「もっとゲームが楽しくなる機能を」。すべて盤面ルールを変えない加点・表示系で、§36 の優先順位(連鎖の気持ちよさ)を損なわない。

## D-019: 特殊ブロックのバランス調整(v4)

- **日付:** 2026-07-20
- **内容:** フィードバック「行上昇が速くなると通常ブロックをほとんど打たず、ボム/プリズムしか打たなくなり、同じ文章を繰り返し打つことになって練習にならない」を受け、2点を修正した。
  1. ボム・プリズムの読み仮名を強制的に short tier(全10件)から選ぶのをやめ、通常ブロックと同じ Tier 分布(short 20% / standard 65% / long 15%、全53件)から選ぶように変更
  2. 盤面全体でボム・プリズムをそれぞれ**同時に最大1個まで**に制限(1行1個の制約は維持)。既存の抽選ロールは、上限に達している種類の分を消費するだけで通常ブロックへフォールバックし、もう片方の出現率を誤って底上げしないようにした
- **理由:** 短文プール(10件)だけを繰り返し引くため文章が単調になり、かつ「短い労力で盤面を大きく消せる」効率の良さから上級者が通常ブロックを無視して特殊ブロックだけを狩る戦略が最適化されてしまっていた。盤面全体で2個(ボム1+プリズム1)までに絞ることで、残り4列以上は通常ブロックの連鎖でしか対処できず、タイピング練習としての多様性が強制的に確保される。

## D-020: ビューポート縮小時のレイアウト保護(v4)

- **内容:** Chromeのブックマークバー表示など、実効ビューポート高さが減った場合に画面上部が到達不能になっていた問題を修正した。原因は `.screen` が `height:100%` 固定+中央寄せで、`body{overflow:hidden}` と組み合わさっていたため、コンテンツがビューポートより高いと中央寄せのはみ出しが上下均等に発生し、上方向のはみ出し分がドキュメント座標マイナスとなってスクロールしても永久に見えなくなっていたこと。
  - `body` を `overflow-y: auto` に変更(下方向のはみ出しは常にスクロールで到達可能にする保険)
  - `.screen` を `justify-content: flex-start` に変更(はみ出しを常に下方向のみへ逃がし、上部が埋もれる経路を構造的に排除)
  - `useFitToViewport` フック(`apps/web/src/hooks/useFitToViewport.ts`)を追加。コンテンツがビューポートに収まらない場合、`transform: scale()` で自動的に縮小する(最小スケール0.55、それ以上縮めても読みにくくなるため下限を設け、それでも収まらない極小ウィンドウはスクロール併用にフォールバック)。ResizeObserver(コンテンツ自身+documentElementの双方を監視)・window resizeイベント・500msポーリングの3系統で検知し、いずれか1つが効かない環境でも追従する
- **理由:** ブックマークバーの表示/非表示はページのコンテンツサイズを一切変えずビューポートだけを変えるため、コンテンツ側だけを監視していると検知できない。検証で使用したブラウザツールの `resize_window` はCDP経由のビューポート上書きで、実ブラウザと異なり `resize` イベントも `ResizeObserver` も発火しないことが判明したため、ポーリングを保険として追加した(実ブラウザでは通常イベントベースの検知だけで十分機能する)。

## D-021: 世界リリース向けスコープ確定(v5準備)

- **日付:** 2026-07-20
- **内容:** 「本格的に世界にリリースする」にあたり、ユーザーと以下4点を確認して確定した。
  1. v1公開スコープはサバイバル+CPU対戦のみ(オンライン1対1・Phase 5は次のアップデート)
  2. ホスティングは未定、提案する(→ Vercel/Netlify/Cloudflare Pages比較を別途提示)
  3. UIは日本語のみ(英語UI/i18nは対象外)
  4. 問題文は現状61件のまま公開し、後日拡充(550件は§26の将来目標として維持)
- **理由:** バックエンド不要な静的サイトとして最速でリリースし、ユーザーの実際の反応を得ることを優先。

## D-022: 画面幅ガード(1024px未満)を追加

- **内容:** `useFitToViewport` の無限スケールに任せず、`SmallScreenGuard`(apps/web/src/components/SmallScreenGuard.tsx)でウィンドウ幅が1024px未満の場合は案内メッセージへ差し替える。
- **理由:** 仕様書§4「推奨最小幅1024px」「スマートフォン最適化はMVP外」を明確に運用するため。無理に縮小表示するとタイピング操作自体が成立しない(物理キーボード前提)ため、案内で止める方が誠実。

## D-023: アクセシビリティ拡充(High Contrast・文字サイズ・OS設定反映)

- **内容:** 仕様書§20の残項目を実装した。
  - `prefers-reduced-motion: reduce` のOS設定を、初回訪問時の Reduced Motion 既定値に反映(`storage.ts` の `defaultSettings()`)
  - High Contrast トグル: Canvas描画(`BoardRenderer`)でブロックを単色塗り+常時枠線+文字の縁取りに変更、HUDのCSSも背景・枠線を強める
  - 文字サイズ選択(標準/大/特大、1.0〜1.3倍): CSSカスタムプロパティ `--font-scale` をHUD主要テキストに適用し、Canvas描画のブロック内文字サイズにも同じ倍率を適用
  - キーボードでのメニュー操作は、全操作を標準 `<button>`/`<input type="checkbox">` で実装済みのため追加対応不要(ネイティブ要素がTab/Enter/Spaceに対応)
- **理由:** 世界へ公開するにあたり、仕様書に明記されたアクセシビリティ項目を欠かさず実装する。

## D-024: ブラウザ互換性の検証範囲

- **内容:** この開発環境で直接操作できるブラウザは Chromium 系(Chrome/Edge相当)のみ。Firefox・Safari の実機検証は行っていない。以下は API サポート状況に基づく書面上の分析。
  - ResizeObserver: Safari 13.1+ / Firefox 69+ で対応済み(問題なし)
  - Web Audio API: 全ブラウザ対応。Safari は自動再生ポリシーが厳しいが、本実装は `AudioContext` をユーザーのクリック操作(サバイバル/対戦開始ボタン)後に初期化しており準拠済み
  - `structuredClone`(対戦成績の保存に使用): Safari 15.4+ / Firefox 94+ / Chrome 98+。仕様書が対象とする「現行安定版」であれば全て対応範囲内
  - CSS カスタムプロパティ(`--font-scale`)・`transform: scale()`: 全モダンブラウザ対応
  - `KeyboardEvent.isComposing`(IME検出): 全モダンブラウザ対応
- **理由:** 実機を用意できない制約下で、根拠のない「動作確認済み」を主張しないため、検証済み範囲(Chromium系・本セッションで多数回実施)と未検証範囲(Firefox・Safari実機)を明確に分けて記録する。
- **推奨:** 公開前に Firefox・Safari(可能なら実iPad/Mac含む)での簡易な手動確認を1回行うこと。特に IME 検出まわりの挙動と、Safari の音声再生解禁タイミングを重点確認。

## D-025: ホスティングをCloudflare PagesからVercelへ変更

- **日付:** 2026-07-20
- **内容:** 当初推奨した Cloudflare Pages(D-024以前の推奨)は、npm workspacesモノレポとの組み合わせで3段階連続の障害(wranglerのワークスペース検出エラー→APIトークン権限不足→ビルドトークン自体の内部不具合)に遭遇し、最後の問題はCloudflare側のUIが提示する修正手順を試しても解消しなかった。原因究明を打ち切り、Vercelへ切り替えたところ即座に成功した。
- **理由:** ローカルビルドは常に成功しており、こちら側の設定・コードに問題がないことは検証済みだった。Cloudflareの新しい「Workers Builds」統合パイプラインがモノレポ構成に対して現状脆弱と判断し、時間を浪費するより実績のあるVercelへ切り替える判断をした。
- **現在の本番URL:** https://type-burst-web.vercel.app/(詳細は docs/DEPLOYMENT.md)
- **今後:** Cloudflareの帯域無制限という利点を活かしたい場合は、`docs/DEPLOYMENT.md` の「経緯」セクションを参照のうえ再挑戦する。`wrangler.toml` はリポジトリに残置しているが現在未使用。

## D-026: 世界ランキング機能の追加(初のバックエンド導入)

- **日付:** 2026-07-20
- **内容:** ユーザー要望により、サバイバルモードの全期間累計ランキングを追加した。プロジェクト初のバックエンド(Vercel Serverless Functions + Vercel KV)。
  - `apps/web/api/scores.ts`: POST(スコア投稿)/GET(TOP100取得)。Vercel KVのsorted setで管理
  - クライアントは初回のランキング登録時のみニックネーム入力を求める(任意・スキップ可、`localStorage`に保存し以降は自動送信)
  - サーバー側バリデーション: スコア上限(100万)・連鎖上限(60)・生存時間上限(6時間)の妥当性チェック、IPベースの簡易レート制限(5秒に1回)
- **既知の制約(意図的な簡略化):** クライアントが申告したスコアをそのまま信用する方式。`game-core` はSeed+入力から決定論的に再現可能な設計のため、将来的には「Seed+入力ログをサーバーに送りgame-coreで再シミュレーションして検証する」方式へ強化できるが、v1では実装コストとのバランスでスコープ外とした。不正スコアが疑われる場合は手動で除去する運用を想定。
- **理由:** 「登録なし」の方針(仕様書§4等)は維持しつつ、ランキング参加だけは任意入力にすることで両立させた。

## D-027: 広告表示の追加(仕様書からの方針転換)

- **日付:** 2026-07-20
- **内容:** 仕様書§28は将来の収益化でも「広告なし」「Pay to Win禁止」と明記していたが、ユーザー要望により画面両サイドへのGoogle AdSense広告表示を追加した。これは明確な方針転換であり、ここに記録する。
  - `apps/web/src/adsConfig.ts` にプレースホルダーのpublisher ID/スロットIDを配置。実際のAdSense審査通過後にこのファイルのみ書き換えれば有効化される
  - 未設定(プレースホルダーのまま)の間は広告を一切読み込まない安全側の実装(`isAdsenseConfigured()`)
  - GDPR等への配慮として同意バナー(`ConsentBanner.tsx`)を実装し、同意("accepted")を得るまでAdSenseスクリプト自体を読み込まない
  - 広告はビューポート幅1450px以上でのみ表示(ゲーム盤面との重なりを避けるため)。ゲームの進行・結果・スコアには一切影響しない(Pay to Winは行わない、利用規約に明記)
- **理由:** ユーザーの明示的な要望。「登録なし」「Pay to Win禁止」など他の原則は維持しつつ、収益化手段としての広告のみ解禁する。
- **今後の対応:** 実際にAdSenseアカウントが承認されたら `adsConfig.ts` を実IDに更新し、可能であればGoogleの公式同意管理ツール(Funding Choices)への切り替えも検討する。

## D-028: ゲーム名を「TYPE BLAST」から「TYPE BURST」へ変更(フルリブランド)

- **日付:** 2026-07-20
- **内容:** ユーザーが「TYPE BLAST」でネット検索したところ類似アプリが複数見つかったため、名前を「TYPE BURST」へ全面変更した。ゲーム内の必殺技名(TYPE BURST、D-013)と同名になるが、意図的なブランディングとして採用(必殺技がそのままゲーム名になった形)。
  - 変更範囲: 表示文言全般・npmパッケージスコープ(`@type-blast/*` → `@type-burst/*`)・GitHubリポジトリ名・Vercelプロジェクト名/公開URL・元の仕様書ファイル名(`TYPE_BLAST_*.md/json` → `TYPE_BURST_*.md/json`)
  - 候補として「Kana」系・「Key」系(KeyStorm等)の造語も検討したが、「大衆受け・英語圏でも日本語圏でも通じる」という基準から絞り込み、最終的に元々「Type」を活かしつつ「Blast」より衝突が少なそうな組み合わせとして「TYPE BURST」を採用した
  - 実際の商標・既存アプリとの重複はAIでは検索できないため、ユーザー自身の確認を前提としている
- **理由:** ユーザーの明示的な要望(検索エンジンでの衝突回避、ブランド差別化)。
- **完了報告(2026-07-20):** GitHubリポジトリ(`shou0903/type-burst`)・Vercelプロジェクト(`https://type-burst-web.vercel.app/`)ともにユーザーによるリネームが完了。ローカルの git remote も追従済み。

## D-029: ランキングのデータストアを Vercel KV から Redis(ioredis)へ変更

- **日付:** 2026-07-20
- **内容:** D-026実装当時の `@vercel/kv` はREST API方式(`KV_REST_API_URL`/`KV_REST_API_TOKEN`)を前提としていたが、`@vercel/kv` は現在非推奨。Vercelダッシュボードの Storage タブも「Vercel KV」自体が無くなり、Marketplace経由の連携に変わっていた。
  - ユーザーが実際に接続したのは Marketplace の「Redis」(Official Redis Cloud連携、Tokyo region・Free 30MBプラン)
  - この連携が発行する環境変数は `REDIS_URL` 1つのみ(TCP接続文字列、RESTではない)
  - `@vercel/kv` は使えないため、`apps/web/api/scores.ts` を `ioredis`(標準的なRedis TCPクライアント)を使う実装に書き換えた
  - サーバーレス関数のウォームインスタンス間で接続を使い回すよう、Redisクライアントをモジュールスコープでキャッシュしている
- **理由:** Vercel側のプロダクト変更(KV廃止・Marketplace移行)に追従。ロジック(sorted setでのランキング管理、バリデーション、レート制限)自体は変更していない。
- **教訓:** Vercelの「公式っぽい名前」の連携でも、REST方式とTCP方式のどちらを提供するかは連携先次第。実際に接続を作り、Environment Variablesの実際の変数名を確認してからコードを合わせる必要がある。

## D-030: Google AdSense 本番設定の完了

- **日付:** 2026-07-20
- **内容:** D-027で実装しておいた広告枠に、実際のAdSense情報を反映した。
  - `apps/web/src/adsConfig.ts`: publisher ID(`ca-pub-5471900652537950`)、左右の広告ユニットID(縦長タイプ・レスポンシブ)を設定
  - `isAdsenseConfigured()`(広告ユニットIDも含めた完全設定判定)と `hasAdsenseClientId()`(publisher IDのみの判定)を分離。サイト所有権確認用メタタグ(`<meta name="google-adsense-account">`)は広告ユニット作成前の審査段階でも表示できるよう、後者だけで判定するようにした
  - Google推奨の設定に合わせ、`data-ad-format="auto"` / `data-full-width-responsive="true"` を使用(固定160×600ではなく、160px幅コンテナ内でレスポンシブに高さが決まる形式)
  - `apps/web/public/ads.txt` を追加(`pub-5471900652537950`宣言。収益計上に必須)
- **理由:** 広告表示に必要な最後のピースを埋めた。ユーザーの同意(ConsentBanner)+ビューポート1450px以上+この設定完了、の3条件が揃って初めて広告が表示される(D-027の安全設計を維持)。

## D-031: CPU対戦中に盤面サイズが変わったり戻ったりする不具合の修正

- **日付:** 2026-07-21
- **内容:** CPU対戦モードで、妨害の送受信・PERFECTコンボ・DANGER警告などのタイミングで画面全体(盤面+HUD)の縮尺が繰り返し変化し、プレイしづらいという報告への対応。
  - **原因:** `useFitToViewport`(画面がビューポートより大きい場合に縮小するフック)はコンテンツの自然な高さ(`scrollHeight`)を`ResizeObserver`で監視し、変化するたびに再計算・再スケールする。`GameScreen.tsx`内の`.streak-badge`(PERFECT×N)・`.cpu-incoming`(CPUへの妨害数▼N)・`.incoming-badge`(妨害接近▼N)・`.danger-badge`(DANGER!)の4つは、対戦中の状態変化に応じて条件付きでDOMへ着脱される実装になっていた。これらが出現/消失するたびに`.screen.game`コンテナの実寸高さが変わり、`useFitToViewport`が反応して盤面全体を拡大縮小していた。
  - 同種の問題は`.hud-typing`(入力欄)については既に`min-height: 118px`で予防済みだったが、後から追加されたこれら4つのバッジには同じ対策が漏れていた。
  - **修正:** 4つのバッジをDOMへの着脱ではなく常時マウントしたうえで、非表示時は`visibility: hidden`(`.badge-reserved.badge-hidden`)にする方式へ変更(`apps/web/src/screens/GameScreen.tsx`, `apps/web/src/styles.css`)。`visibility: hidden`はレイアウト上の領域を保持したまま見た目だけ隠すため、表示/非表示に関わらずコンテナの高さが変化しなくなる。
  - ブラウザで`player.danger`等を直接書き換えて検証し、バッジのON/OFFに関わらず`el.scrollHeight`と`transform: scale(...)`が完全に一定であることを確認済み。
- **理由:** 画面が動いてしまう体感の根本原因は「バッジの着脱によるレイアウト変化」であり、`useFitToViewport`側の再計算ロジック自体は(ブックマークバーの表示切替など)正しく必要な機能だったため、原因側(バッジの実装)を直す方針とした。

## D-032: サバイバルモードに難易度選択とランキング補正を追加

- **日付:** 2026-07-21
- **内容:** 「上級者でないと続かず、初心者はすぐ終わってしまう」というフィードバックを受け、サバイバルモードに難易度(のんびり/ふつう/本気)を追加した。
  - 難易度間で公平になるよう、盤面サイズ・出現ロジックは揃えたまま**行上昇の速さのみ**を変える(`packages/game-core/src/config.ts`の`survivalDifficulty`)。CPU対戦の`CpuDifficulty`と同様のパターンで`PlayerCore`へ`RiseConfig`を注入する仕組みは元から存在しており、それを流用した。
  - 世界ランキングは難易度に関わらず同一のものを維持したいという要望のため、ランキング送信時のみサーバー側(`apps/web/api/scores.ts`)でスコアに難易度別の補正係数(easy×0.65 / normal×1.0 / hard×1.4)を掛けた値をランキングの並び順に使う。生スコア(`rawScore`)も併せて保存・表示し、ランキング画面には難易度チップを表示して透明性を確保した。
  - ローカルの自己ベスト・前回比較は難易度ごとに分離(`apps/web/src/storage.ts`の`bestScore`に`difficulty`引数を追加)。異なる難易度の記録を混ぜて比較しても意味がないため。
  - 難易度未指定(旧データ・API呼び出し)は`normal`として扱い後方互換を維持。
- **理由:** 初心者の離脱を防ぎつつ、上級者向けの手応えも維持したい。ランキングを分割せず1本にしたいという要望に対しては、公平性を保つためのスコア補正という形で応えた。
- **今後の課題:** 補正係数(0.65 / 1.4)と行上昇パラメータは実プレイデータが無い状態での初期値。実際のスコア分布を見て調整が必要。

## D-033: サバイバル難易度の実現方法を「行上昇の速さ」から「文章の長さ」へ変更

- **日付:** 2026-07-21
- **内容:** D-032で実装した難易度(easy/normal/hard)は行上昇の速さ(RiseConfig)を変える方式だったが、本番反映後にユーザーから「難易度を下げれば行上昇が十分遅くなり、積み上がらないまま無限に生存してスコアを無限に稼げてしまう」という指摘を受けた。
  - **対応:** 行上昇の速さ(`config.survivalRise`)を難易度に関わらず完全に共通の1種類に戻し、代わりに1ブロックあたりに要求される文章の長さの配分(`tierRatio`: short/standard/long)を難易度ごとに変える方式に変更した。
    - easy: `{ short: 0.6, standard: 0.35, long: 0.05 }`(短い文章が多く、素早くさばける)
    - normal: `{ short: 0.2, standard: 0.65, long: 0.15 }`(既存のデフォルトのまま)
    - hard: `{ short: 0.05, standard: 0.45, long: 0.5 }`(長い文章が多く、要求打鍵数が多い)
  - `packages/game-core/src/survival.ts`: `PlayerCore`へ渡す`config`を`{ ...config, tierRatio: profile.tierRatio }`で複製し、`rise`は常に`config.survivalRise`(共通)を渡すよう変更。CPU対戦(`DuelGame`)の`tierRatio`には影響しない。
  - スコア倍率(D-032で導入)は維持。行上昇ペースという「終了までの上限」が全難易度共通になったことで、無限にスコアを稼げる問題自体は構造的に解消された。
- **理由:** 「盤面が積み上がるペース」という難易度の根幹部分を難易度間で変えてしまうと、緩い設定側の上限が事実上なくなってしまう。文章の長さで難易度を調整すれば、上限(行上昇ペース)は共通のまま「同じ時間内にどれだけ多くのブロックをさばけるか」という体感難易度だけを変えられる。
- **教訓:** 難易度調整は「終了条件に影響するパラメータ」と「プレイの快適さに影響するパラメータ」を分けて考える必要がある。前者を難易度間で変えると、ゲームの根本的なバランス(このゲームでは「無限には生存できない」という前提)が崩れる。

## D-034: 正式アイコンの導入 + 難易度表記の見直し

- **日付:** 2026-07-21
- **内容:**
  - ユーザーから提供された正式アイコン画像(`apps/web/public/app icon.png`)から favicon(32px/512px)・apple-touch-icon(180px)・OGP/Twitterカード画像(512px)を生成し、`index.html`に反映。従来の仮のインラインSVG favicon、および「ホスティング確定後に追加」で保留していた`og:image`/`og:url`をここで正式に埋めた。
  - サバイバル難易度の表記をひらがな(のんびり/ふつう/本気)から「初級/中級/上級」へ変更。CPU対戦の難易度表記もひらがな(よわい/ふつう/つよい)から漢字(弱い/普通/強い)へ変更。両者は別々の概念(自分のペース vs 対戦相手の強さ)のため、ラベル体系はそれぞれ独立して管理している。
- **理由:** ブランドアイコンの正式反映と、表記の分かりやすさ・一貫性向上(ユーザー指示)。

## D-035: インタラクティブなチュートリアル機能の追加

- **日付:** 2026-07-21
- **内容:** ランディング画面の「遊び方」テキストだけでは初見の操作理解が難しいという課題に対し、実際に手を動かして覚えられるチュートリアルモードを追加した。
  - `packages/game-core/src/tutorial.ts`: `TutorialGame`クラス。全7ステップ(①基本操作 ②連鎖 ③TYPE BURST ④ボム ⑤プリズム ⑥ALL CLEAR ⑦完了)を、各ステップ専用に組んだ決め打ちの盤面で1つずつ体験させる。実際の`PlayerCore`(本編と全く同じ入力・連鎖・スコア計算ロジック)をそのまま使うため、チュートリアルで見た挙動と本編の挙動が食い違うことがない。
  - `PlayerCore.loadTutorialBoard()`(新規)で盤面を任意の内容に差し替え可能にした。スコア・ランキングには一切影響しない。
  - `PlayerCore.pauseRise`(新規フラグ)で行上昇だけを止める。`frozen`は入力自体も止めてしまうため使えず、新設した。
  - 各ステップの達成判定はゲーム内イベント(`blocksCleared`のchain/cause、`burstFired`、`allClear`)を監視する方式。達成するまで「次へ」は押せない(`nextStep()`内でガード)。
  - `apps/web/src/game/GameController.ts`: `GameMode`に`{ type: "tutorial" }`を追加。`SurvivalGame`/`DuelGame`と同列で`TutorialGame`を保持し、既存のレンダリング・入力処理パイプラインをそのまま再利用(IME検知やペースト禁止などを重複実装しない)。
  - `apps/web/src/screens/GameScreen.tsx`: ステップ番号・タイトル・説明文・「次へ/スキップ」ボタンを表示する`tutorial-banner`を追加。行上昇が常に止まっているため、意味を持たない「NEXT ROW」表示はチュートリアル中のみ非表示にした。
  - ステップ切り替え時に前のステップの演出(ALL CLEARのポップアップ等)が次のステップへ残ってしまう問題があったため、`BoardRenderer.clearEffects()`(新規)でポップアップ・リング・パーティクルをリセットしてから次のステップへ進むようにした。
  - `packages/game-core/test/tutorial.test.ts`: 全ステップの達成条件(2連鎖・TYPE BURST発動・ボム/プリズムの発動・ALL CLEAR)と、未達成時に`nextStep()`が進まないことを検証。
- **理由:** 「はじめての方はこちら」を明確に案内し、テキストを読むだけでなく実際に体験させることで、離脱しやすい初回プレイの理解度を高める。

## D-036: プレイ中BGMの追加

- **日付:** 2026-07-21
- **内容:** プレイ中(サバイバル・CPU対戦・チュートリアル)にループ再生するBGMを追加した。タイトル画面では再生しない(ユーザー要望)。
  - 曲: "Clockwork Meditation electronic vibes" by NiKneT_Art(Pixabay Content License、Hypnotic/Minimalist/Electronic)。既存の効果音合成(Web Audio API、音源ファイルなし)とは別に、初めて実ファイルの音源を導入した。トレーラー動画で使った曲(MFCC)とは別の曲をあえて選定し、「ゲーム自体を邪魔しないオリジナリティのある曲」というユーザー要望に沿った(アーケード風の定番曲ではなく、ミニマルで持続的に聴いても疲れにくい曲調)。
  - 元音源は256kbps/5MBだったため、128kbpsへ再エンコードし約2.5MBに圧縮して`apps/web/public/audio/bgm-clockwork.mp3`に配置。
  - `SoundEngine`(`apps/web/src/audio/SoundEngine.ts`)に`playBgm()`/`stopBgm()`/`setBgmEnabled()`を追加。効果音合成用のAudioContextとは別に、素の`<audio loop>`要素で再生する(BGMは長尺ループのため、Web Audio経由でバッファ管理するより単純で確実)。
  - 効果音(`soundOn`)とは独立した`bgmOn`設定を追加(`storage.ts`)。ランディング画面に「BGM」チェックボックスを追加。
  - `GameScreen.tsx`のマウント時に`playBgm()`、アンマウント時に`stopBgm()`を呼び、プレイ中のみ鳴る設計にした。設定変更はApp.tsxの`useEffect`で`setBgmEnabled()`を呼び、再生中でも即座に反映されるようにした(単純なプロパティ代入だと再生中の音声を止められないため)。
- **理由:** ゲームの雰囲気作りとプレイ体験の向上。効果音とは切り離して個別にON/OFFできるようにし、音楽の好みが分かれても柔軟に対応できるようにした。
- **取り消し(同日):** 本番反映・動作確認の直後にユーザーから「やっぱりBGMいらない」との判断があり、本機能一式(`playBgm`/`stopBgm`/`setBgmEnabled`、`bgmOn`設定、ランディング画面のBGMチェックボックス、音源ファイル)を撤去した。チュートリアル機能(D-035)は同一コミットに含まれていたが影響を受けず、そのまま維持している。

## D-037: アクセス解析(Vercel Web Analytics)の追加

- **日付:** 2026-07-21
- **内容:** サービス運営者がアクセス状況を把握できるよう、Vercel Web Analyticsを導入した。
  - `apps/web`に`@vercel/analytics`を追加し、`AppRoot.tsx`の最上位に`<Analytics />`を常時マウント。同意バナー(広告用Cookie同意)とは無関係に、Cookie不使用・個人を特定しない集計のみのため常時計測する設計にした。
  - `apps/web/public/privacy.html`を改訂し、「Google Analyticsなどのアクセス解析ツールは現時点で組み込んでいません」という従来の記載を、実際にVercel Web Analyticsを利用している旨に更新。あわせて、D-029で移行済みだった「Vercel KV」という旧い表記が1箇所残っていたのを「Redis」に修正(内容自体は既にD-029で対応済み、表記の直し忘れ)。
  - 既存のVercelプロジェクトにそのまま乗る形のため、新規アカウント作成は不要。ダッシュボードでの有効化(Project → Analytics タブ)はユーザー側の操作が必要。
- **理由:** GA4等より導入・運用コストが低く(Cookie同意バナーの追加改修が不要)、既存のVercelダッシュボードでそのまま閲覧できるため、運営の手間を最小化できる。より詳細なイベント計測(ファネル分析等)が必要になった場合はGA4等の追加導入を別途検討する。

## D-038: サバイバル難易度の再修正(行上昇の速さを難易度ごとに復活)

- **日付:** 2026-07-21
- **内容:** D-033で「行上昇の速さは全難易度共通、文章の長さ(tierRatio)だけで難易度を付ける」方式に変更したが、本番反映後にユーザーから「難易度の調整が全然できていない。初級の単語が長すぎて初心者は全然時間ない」との指摘を受けた。
  - **原因分析:** 文章の長さだけを短くしても、そもそも1行あたりに与えられる時間(行上昇の間隔)自体がnormalと同じままでは、初心者がタイピングに慣れるための猶予が根本的に不足する。実際のフレーズデータ(`packages/phrase-content`)を確認したところ、short tierは全61件中10件のみで、そのshort tierですらローマ字換算で8〜12文字程度あり、文章を短くする調整だけでは初心者の「読んで打ち始めるまでの時間不足」を解消しきれないことを確認した。
  - **対応:** `packages/game-core/src/config.ts`の`SurvivalDifficultyProfile`に`rise: RiseConfig`を復活させ、行上昇の速さを再び難易度ごとに変える。ただし、D-032で問題になった「easyの行上昇が遅すぎて無限に生存できる」という状態を再発させないよう、以下の設計にした。
    - **開始間隔(`startIntervalMs`)と加速度(`accelPerSecondMs`)は難易度間で大きく差をつける**(easy: 9500ms開始・10ms/秒、normal: 5500ms開始・25ms/秒、hard: 4200ms開始・32ms/秒)。序盤の猶予が初心者の生存体験を大きく左右するため、ここを主な調整レバーにした。
    - **最終的な最短間隔(`minIntervalMs`、floor)はeasyとnormalで差を小さく保つ**(easy: 2800ms、normal: 2400ms、hard: 1700ms)。D-032のeasy(4000ms)はnormal(2400ms)との差が大きすぎて無限生存の温床になっていたため、今回は差を400msに抑え、プレイし続ければ最終的にnormal相当の圧力へ収束するようにした。
    - `tierRatio`によるD-033の調整(easyは短い文章が多い)は維持しつつ、easyをわずかにさらに短文寄りに調整(`{ short: 0.6, standard: 0.37, long: 0.03 }`、旧`{0.6, 0.35, 0.05}`)。
    - スコア倍率はeasyの実質的な難易度緩和分を反映し`0.65`→`0.6`に調整(`packages/game-core/src/config.ts`と`apps/web/api/scores.ts`の両方を同期して変更)。
  - `packages/game-core/src/survival.ts`: `PlayerCore`へ渡す`rise`を、D-033時点の`config.survivalRise`(共通)から`profile.rise`(難易度ごと)に戻した。`GameConfig`から`survivalRise`フィールド自体を削除し、各難易度の`survivalDifficulty[*].rise`に一本化。
  - `packages/game-core/src/tutorial.ts`: `TutorialGame`は`pauseRise`により行上昇自体が常に停止しているため実質無関係だが、削除した`config.survivalRise`の参照先を`config.survivalDifficulty.normal.rise`に差し替えた。
  - `packages/game-core/test/survival.test.ts`: D-033で追加した「行上昇の速さは難易度に関わらず共通」という前提のテストを削除し、「開始直後はeasyの方が行上昇に大きな余裕がある(normalの1.5倍以上)」「長時間経過後の最短間隔はeasyとnormalで差が小さい(500ms以内)」という新しい前提を検証するテストに置き換えた。
- **理由:** 難易度調整には「終了条件の上限(floor)」と「序盤の体感的な猶予(start/accel)」という2つの異なるパラメータがあり、D-033はこれらを区別せず両方を難易度間で共通化してしまったため、初心者救済という本来の目的を達成できていなかった。floorの差を小さく保つことでD-032の無限生存問題を再発させずに、序盤の猶予だけを難易度ごとに大きく変えることで両立を狙う。
- **教訓:** 「無限に有利になる」問題と「序盤に十分な猶予を与える」要求は本質的に別物であり、片方を潰すために両方を犠牲にする必要はない。パラメータを分解し、どの部分が終了条件(上限)に効き、どの部分が体感難易度(序盤の余裕)に効くかを切り分けて設計すべきだった。
- **取り消し(同日):** ユーザーから「上昇速度は変えないでっていったよね。シンプルに、ブロック内の単語を短くしてよ」と明確な訂正を受けた。D-033で確定していた「行上昇の速さは全難易度共通」という制約を、本Decisionでは筆者の判断だけで覆してしまっていた。ユーザーの意図は当初から一貫して「盤面が積み上がるペースは変えず、1ブロックあたりの文字数だけで難易度を付ける」であり、行上昇速度の変更はその指示に反していた。D-039で行上昇速度を再びD-033の共通設定に戻し、文章の長さ側の調整を強化する形で問題に対応し直した。

## D-039: サバイバル難易度をD-033方式(行上昇共通・文章の長さのみ)に戻し、easyの文章をさらに短く

- **日付:** 2026-07-21
- **内容:** D-038は「初級が難しすぎる」というユーザーの指摘に対し行上昇速度を難易度ごとに変える方向で対応したが、これはD-033でユーザーが明確に指定した「降ってくる時間はどれも同じで」という制約に反していた。ユーザーから「上昇速度は変えないでっていったよね。シンプルに、ブロック内の単語を短くしてよ」と訂正を受け、D-033の設計方針(行上昇共通・tierRatioのみで難易度を付ける)に戻した上で、easyの文章の短さを大幅に強化した。
  - `packages/game-core/src/config.ts`: `SurvivalDifficultyProfile`から`rise: RiseConfig`を削除し、`GameConfig`に`survivalRise: RiseConfig`(全難易度共通、D-033時点の値: 開始5500ms・最短2400ms・加速25ms/秒)を復活。
  - easyの`tierRatio`を`{ short: 0.6, standard: 0.37, long: 0.03 }`から`{ short: 0.95, standard: 0.05, long: 0 }`へ大幅変更し、ほぼ全てのブロックがshort tierの短い文章になるようにした(1ブロックあたりの打鍵数を実質的に大きく削減)。normal/hardのtierRatioは変更なし。
  - スコア倍率はeasyの難易度緩和幅拡大を反映し`0.6`→`0.5`に調整(`packages/game-core/src/config.ts`と`apps/web/api/scores.ts`を同期)。
  - `packages/game-core/src/survival.ts`・`packages/game-core/src/tutorial.ts`: `PlayerCore`へ渡す`rise`をD-038の`profile.rise`(難易度ごと)から`config.survivalRise`(共通)へ戻した。
  - `packages/game-core/test/survival.test.ts`: D-038で追加した「easyの行上昇に序盤の余裕がある」テストを削除し、D-033の「行上昇の速さは難易度に関わらず共通」テストを復活。加えて「easyはほぼ全てshort tierになる(0.85超)」テストを新規追加し、D-039の意図を固定した。
- **理由:** 難易度の実現方式(行上昇 vs 文章の長さ)は既にD-033でユーザーと合意済みの制約であり、実装側の判断だけで変更すべきではなかった。「初級が難しすぎる」という症状に対しては、tierRatioの調整幅がまだ不十分だった(easyでも標準/長文が約4割残っていた)ことが本当の原因であり、行上昇速度に手を付ける必要はなかった。
- **教訓:** ユーザーが明示的に指定した設計上の制約(「行上昇は共通で」)は、後続の不具合修正であっても勝手に緩めてはならない。同じ症状に対して複数の直し方がありうる場合、まず「以前ユーザーが除外した選択肢ではないか」を確認してから対応方針を決めるべきだった。

## D-040: 単語レベルの短さを持つ新tier「micro」を新設(寿司打を参考に)

- **日付:** 2026-07-21
- **内容:** D-039でeasyのtierRatioをshort tier中心(95%)へ寄せたが、ユーザーから「全然短文じゃない。短文ってのはほんとに3文字とか4文字とかもっと短いの。寿司打とかを参考にしてほしい」との指摘を受けた。既存のshort tierは「音が聞こえる」のような自然な短文(5〜7モーラ)であり、寿司打のような単語単体(例: 「犬」「花」)の短さとは根本的に異なるものだった。
  - **原因分析:** `packages/phrase-content`の文章は全て設計時から「自然な日本語の文」として作られており、最短のshort tierでも主語・助詞・動詞を含む文であるため、5〜7モーラが実質的な下限だった。文単位のtierを寄せるだけではユーザーが期待する「単語1つをポンと打つ」短さには届かない。
  - **対応:** 文ではなく単語1語を1ブロックとする新tier`micro`(2〜4モーラ)を新設した。
    - `packages/phrase-content/src/types.ts`: `PhraseTier`に`"micro"`を追加。
    - `packages/phrase-content/src/phrases.ts`: 「犬」「猫」「花」「空」「魚」「車」「電話」「時計」など、寿司打を参考にした2〜4モーラの単語を24件追加(category: "単語")。
    - `packages/phrase-content/src/index.ts`: `TIER_MORA_RANGE`に`micro: { min: 2, max: 4 }`を追加。
    - `packages/game-core/src/player.ts`: `pickTier()`を4tier(micro/short/standard/long)の累積分布に対応させた。
    - `packages/game-core/src/config.ts`: easyの`tierRatio`を`{ micro: 0.75, short: 0.2, standard: 0.05, long: 0 }`に変更し、単語レベルの短さ(micro)が中心になるようにした。normal/hardは`micro: 0`のまま据え置き(文章ベースの体験を維持)。
    - スコア倍率はeasyの難易度緩和幅拡大を反映し`0.5`→`0.45`に調整(`config.ts`と`apps/web/api/scores.ts`を同期)。
  - `packages/phrase-content/test/content.test.ts`・`packages/game-core/test/survival.test.ts`: 新tierの存在確認・easyがmicro中心になること・easy/normal/hardの平均的な文章の長さ(tierの重み)が段階的に長くなることを検証するテストに更新。
- **理由:** 「短い」の基準がユーザーとズレていた。文章を短くする発想では、そもそも「文」である以上の下限を超えられない。単語単体という新しいカテゴリを作ることで、初めてユーザーが求める寿司打的な短さを実現できる。
- **教訓:** 既存のコンテンツ設計(全て自然な文章)の枠内でパラメータ調整を繰り返しても、ユーザーが求める体験が既存カテゴリの外にある場合は届かない。「短くして」という要求を額面通りの延長線上でしか捉えず、具体的な参考例(寿司打)を早い段階で確認していれば、D-039より前にこの設計に気づけた可能性がある。

## D-041: 初級の「無限に生存できる」問題を修正し、ランキングを難易度別に分離

- **日付:** 2026-07-21
- **内容:** D-040でeasyをmicro tier(単語レベル)中心にしたところ、ユーザーから「初級だと一生できてしまう。そこら辺のゲームバランスの調整をもっと考えてしてほしい」「ランキングはやっぱり難易度別に分けたほうがいい」との指摘を受けた。これは2つの独立した修正として対応した。
  - **①ゲームバランス(無限生存の修正):**
    - **原因分析:** 行上昇の間隔には下限(`minIntervalMs`、floor)があり、D-040時点では2400msだった。floorに達すると行上昇はそれ以上速くならないため、1行(6ブロック)をfloor間隔内に安定してさばき続けられる難易度は理論上無限に生存できてしまう。easyのmicro tier(2〜4モーラの単語)は非常に速く打てるため、2400ms/6ブロック=平均400ms/ブロックというfloorのペースを上回るのが可能になり、実際に「一生できる」状態が生まれていた。normal/hardは文章が長く、floorのペースにいずれ追いつかれるため問題化していなかった。
    - **対応:** `packages/game-core/src/config.ts`の共通`survivalRise`のfloorを2400ms→1200msへ引き下げ、加速度も25→30ms/秒に強化した。**floorは全難易度共通のパラメータであり、これを調整してもユーザーが指定した「難易度間で行上昇速度を変えない」制約には抵触しない**(難易度差はtierRatioのみで維持)。これにより、micro中心のeasyであっても長時間プレイすれば必ず盤面が積み上がり終了するようにした。
    - この調整は実プレイデータに基づく厳密な検証ではなく、理論的な妥当性(1行あたりの平均処理時間の見積もり)に基づく暫定値。実際のプレイで依然として無限生存に近い状態が起きる場合は、floorのさらなる引き下げや、片手落ちにならないよう他の指標(例: 選択ブロックの視認・ロック時間)も含めた見直しが必要になる可能性がある。
  - **②ランキングの難易度別分離:**
    - **対応:** これまでは1本の「全難易度共通ランキング」に対し`scoreMultiplier`(D-032由来のスコア補正係数)を掛けて公平性を保つ設計だったが、難易度ごとに完全に別のランキングへ分離する方式に変更した。
      - `apps/web/api/scores.ts`: Redisのsorted setキーを`leaderboard:survival:alltime`の1本から`leaderboard:survival:alltime:{easy|normal|hard}`の3本に分離。GETエンドポイントは`?difficulty=`クエリパラメータを受け取り(未指定時はnormal)、該当難易度のランキングのみ返す。POST時のスコア補正(`SCORE_MULTIPLIER`)は不要になったため撤去し、素点をそのままランキングに使用する。
      - `packages/game-core/src/config.ts`・`survival.ts`・`types.ts`: 難易度別ランキングにより不要になった`scoreMultiplier`(`SurvivalDifficultyProfile`・`SurvivalSummary`)を削除。
      - `apps/web/src/ranking.ts`: `fetchTopScores(difficulty, limit)`に難易度引数を追加。`RankingEntry`から`rawScore`(補正前後の区別)を削除しシンプルな`score`のみに。
      - `apps/web/src/screens/RankingScreen.tsx`: ランキング画面に初級/中級/上級のタブ(既存の`chip`/`chip-active`スタイルを流用)を追加し、選択した難易度のランキングのみ表示。テーブルの「難易度」列(旧`ranking-difficulty-chip`)は不要になったため削除。
    - 旧・共通ランキングのRedisデータ(`leaderboard:survival:alltime`キー)は削除せずそのまま残し、新しい難易度別キーへの移行は行わない(過去データは表示されなくなるが、破壊的操作を避けるため放置)。
- **理由:** ①は「常に共通の行上昇ペースがある以上、いずれ誰でも敗北する」という設計の大前提が、tierRatio側の調整幅が広がったことで崩れていたための構造的な修正。②は「難易度が違えば比較対象として意味がない」というユーザーの直感的な指摘であり、スコア補正による正規化よりも単純に分離した方が納得感が高い。
- **教訓:** 「行上昇速度は共通」という制約と「floor(共通パラメータ)の値」は別物であり、難易度ごとの体験(tierRatio)を大きく変えるたびに、共通パラメータ側がその変化に対して依然として妥当かを再検証する必要がある。また、スコアの公平性は「補正して1本化する」よりも「土俵を分ける」方が、ユーザーにとって直感的で説明不要な場合が多い。

## D-042: 語彙数の大幅追加と、難易度間の語彙帯を重ねない再設計

- **日付:** 2026-07-21
- **内容:** ユーザーから「もっと単語を追加してほしい」「初級、中級、上級の単語の差をもっと明確にしてほしい」との指摘を受けた。
  - **①語彙数の追加:** `packages/phrase-content/src/phrases.ts`に56件追加し、全体を85件→141件に拡充した。
    - micro(単語): 24件→40件(「本」「傘」「机」「鍵」「靴」「果物」「野菜」「氷」「虹」「春」「夏」など16件追加)
    - short(短文): 10件→25件(「風が吹く」「席を立つ」「電話に出る」など15件追加)
    - standard(標準文): 33件→48件(「朝ごはんを食べる」「予定を確認する」など15件追加)
    - long(長文): 18件→28件(「休みの日はゆっくり体を休める」「静かな図書室で集中して勉強する」など10件追加)
    - `docs/CONTENT_GUIDELINES.md`のTier表・目標件数表をmicro tier追加と最新件数に合わせて更新(D-040でmicroを追加した際にこのドキュメントの更新が漏れていたのも合わせて修正)。
  - **②難易度間の差の明確化:** `packages/game-core/src/config.ts`の`survivalDifficulty[*].tierRatio`を再設計した。
    - **原因分析:** D-040/D-041時点ではnormal(`short 0.2 / standard 0.65 / long 0.15`)とhard(`short 0.05 / standard 0.45 / long 0.5`)がどちらもstandard tierに大きく依存しており、実際に出現する文章の多くが同じstandardプールから選ばれるため、両者の体感差が薄かった。
    - **対応:** 各難易度が主に使うtierの範囲をなるべく重ねないよう変更した。
      - easy: `{ micro: 0.8, short: 0.2, standard: 0, long: 0 }` (standard/longは一切出さず、ほぼ単語のみ)
      - normal: `{ micro: 0, short: 0.35, standard: 0.55, long: 0.1 }` (短文〜標準文中心、長文はわずか)
      - hard: `{ micro: 0, short: 0, standard: 0.25, long: 0.75 }` (標準文は控えめにし、長文中心)
  - `packages/game-core/test/survival.test.ts`: 既存の「easyはmicro tier中心」「easy<normal<hardの順に平均的な文章の長さが長くなる」テストは新しい比率でも成立することを確認済み(閾値の変更は不要だった)。
- **理由:** ①は語彙の反復感を減らし単純に「もっと単語がほしい」という要望に応えるもの。②は「同じstandardプールを共有している限り、比率をどれだけ変えても体感差は頭打ちになる」という構造的な問題への対応で、主要な語彙帯そのものをずらすことで初めて三段階の違いが明確になる。
- **教訓:** tierRatioの比率だけを調整しても、複数の難易度が同じtierに大きく依存している限り体感差には限界がある。「難易度を分ける」ときは、各難易度が使うtierの集合自体が重ならないように設計する方が効果的。

## D-043: 語彙数をさらに拡充(141件→201件)

- **日付:** 2026-07-21
- **内容:** D-042の方向性(語彙数の追加、難易度間の語彙帯の分離)についてユーザーから「いいですね。その方向で、単語をもっと拡充して」と肯定を得たため、同じ方針で`packages/phrase-content/src/phrases.ts`にさらに60件追加した(141件→201件)。tierRatioの再設計(D-042)はそのまま維持し、今回は語彙数のみを追加した。
  - micro(単語): 40件→60件(「秋」「冬」「森」「石」「家」「庭」「橋」「駅」「道」「店」「銀行」「公園」など20件追加)
  - short(短文): 25件→40件(「雪が降る」「鍵をかける」「窓を閉める」「宿題をする」「駅に着く」など15件追加)
  - standard(標準文): 48件→63件(「朝早く目を覚ます」「バス停で友達と会う」「先生に質問する」など15件追加)
  - long(長文): 28件→38件(「電車が遅れて少し焦った」「失敗しても諦めずにもう一度挑戦する」「毎日少しずつ上達していく」など10件追加)
  - `docs/CONTENT_GUIDELINES.md`の目標件数表を最新件数(micro 60 / short 40 / standard 63 / long 38)に更新。
  - `npm test`で全件の検証(ID・表示文の重複なし、Tierとモーラ数の一致、読み仮名の入力可能性、表示文20文字以内)を確認済み。
- **理由:** D-042での語彙拡充・難易度差の再設計がユーザーの意図と合致していたことが確認できたため、同じ方針を継続して語彙の反復感をさらに減らす。
- **教訓:** なし(D-042の方針がそのまま有効だったことを確認した回。設計変更は行っていない)。

## D-044: 中級・上級のtierRatioを微調整(平均的な文章の長さを短縮)

- **日付:** 2026-07-21
- **内容:** D-042/D-043の比率で実際に触ったユーザーから「初級はいい感じ。中級はちょっと難しいから、もう少し平均文字数落として、今の中級と上級の間くらいが上級にちょうどいいと思う」とのフィードバックを受けた。easyの比率はそのまま維持し、normal/hardのみ調整した。
  - normal: `{ short: 0.35, standard: 0.55, long: 0.1 }` → `{ short: 0.5, standard: 0.45, long: 0.05 }`。shortの比率を増やし、standard/longを減らして平均的な文章の長さをやや短くした。
  - hard: `{ short: 0, standard: 0.25, long: 0.75 }` → `{ short: 0, standard: 0.7, long: 0.3 }`。longの比率を大きく下げてstandardを増やし、旧normal(調整前)と旧hard(調整前)のちょうど中間程度の長さになるようにした。
  - `packages/game-core/test/survival.test.ts`の「easy<normal<hardの順に平均的な文章の長さが長くなる」テストは、新しい比率でも順序が保たれることを確認済み(閾値の変更は不要だった)。
- **理由:** ユーザーの実プレイ後の直接的なフィードバックに基づく調整。easyは好評だったため変更せず、normal/hardのみ対象を絞って調整した。
- **教訓:** なし(既存設計の範囲内でのパラメータ微調整)。

## D-045: X(Twitter)のリンクカード画像が表示されない問題への対応

- **日付:** 2026-07-21
- **内容:** ユーザーからX(Twitter)の投稿作成画面で、リンクカードの画像枠(左側)が空白のまま何も表示されないとの報告を受けた(スクリーンショット添付)。
  - **調査:** `og:image`/`twitter:image`(`https://type-burst-web.vercel.app/og-image.png`)は`curl`で200 OK・正しい`Content-Type: image/png`・512×512のアイコン画像であることを確認済みで、コード・画像自体には問題がなかった。
  - **原因分析:** git履歴を確認したところ、`og:image`/`twitter:image`メタタグとアイコン画像ファイルは同じコミット(D-034)で追加されており、それ以前のコミットでは`<!-- og:image / og:url はホスティング確定後に追加 -->`というコメントのみでOGP画像タグ自体が存在しなかった。D-034より前にXのクローラーが一度でもこのURLをクロールしていた場合、「画像なし」のカード情報がXの内部キャッシュに保存され、その後D-034で画像を追加してもXがキャッシュを勝手に更新することはないため、古い「画像なし」のキャッシュが投稿作成画面に表示され続けていたと推測される。
  - **対応:** `apps/web/index.html`の`og:image`/`twitter:image`のURLに`?v=2`というクエリパラメータを付与した(`https://type-burst-web.vercel.app/og-image.png?v=2`)。これによりXのクローラーにとって未知の新しいURLとなるため、次にこのURLの投稿を作成した際に確実に新規クロールが行われ、正しい画像が表示されるようになることを狙った。
- **理由:** コード・画像自体は既に正しかったため、実装を直す問題ではなくXのカードキャッシュを強制的に更新させる必要があった。Xの公式カードバリデータ(cards-dev.twitter.com/validator)は現在ログイン必須かつ挙動が不安定なため、URLを変えて未キャッシュの状態から再クロールさせる方が確実。
- **教訓:** OGP/Twitterカード用の画像は、実際にホスティングし公開URLが確定する前の段階でメタタグだけ先に置いておくと、SNS側のクローラーが「画像なし」の状態を先にキャッシュしてしまうリスクがある。今後同様にメタタグを追加する際は、画像ファイルが実際にデプロイ済みであることを確認してからメタタグを有効化する(またはコメントアウトのまま保留する)方が安全。

## D-046: 語彙数を大幅に拡充(201件→321件)

- **日付:** 2026-07-21
- **内容:** ユーザーから「単語をもっともっと増やしてほしい。今は全然少ないから1ゲームの中で何回も同じ単語を入力することがある」との指摘を受けた。D-042/D-043で201件まで拡充していたが、サバイバル1プレイで数十〜100個以上のブロックを処理することを考えると、特に難易度ごとに実際に使われるプール(easy: micro+short中心で約170件、normal: short+standard中心で約163件、hard: standard+long中心で約151件)はまだ反復を感じるには不十分だった。そこで`packages/phrase-content/src/phrases.ts`に120件を追加し、全体を201件→321件へ拡充した。
  - micro(単語): 60件→100件(「蟻」「蝶」「亀」「象」「熊」「羊」「岩」「滝」「湖」「島」「畑」「米」「塩」「砂糖」「牛乳」「光」「影」「音」「声」など40件追加)
  - short(短文): 40件→70件(「雲が流れる」「音が鳴る」「手を挙げる」「顔を洗う」「靴下を履く」「眼鏡をかける」など30件追加)
  - standard(標準文): 63件→93件(「電車の中で本を読む」「銀行でお金を下ろす」「教室で先生を待つ」「誕生日を祝う」など30件追加)
  - long(長文): 38件→58件(「電車が混んでいて座れなかった」「新しい教科書を開いて勉強を始める」「季節が変わると街の景色も変わる」など20件追加)
  - `docs/CONTENT_GUIDELINES.md`の目標件数表を最新件数(micro 100 / short 70 / standard 93 / long 58)に更新。
  - `npm test`で全321件の検証(ID・表示文の重複なし、Tierとモーラ数の一致、読み仮名の入力可能性、表示文20文字以内)を確認済み。tierRatio(D-044)は変更していない。
- **理由:** 語彙プールが少ないと、特に周回数の多いeasy(micro中心)やプレイ時間の長いサバイバル全般で同じ単語・文章に何度も遭遇し、パズルとしての新鮮さが失われる。CONTENT_GUIDELINES.mdのMVP目標(short 100 / standard 300 / long 100)にはまだ届いていないが、大きく前進させた。
- **教訓:** なし(既存の語彙追加パターンを踏襲した拡充)。

## D-047: 語彙数をさらに大幅に拡充(321件→580件)

- **日付:** 2026-07-21
- **内容:** D-046の直後、ユーザーから「全然足りない。もっと。あと5倍は欲しい」との強いフィードバックを受けた。D-046(+120件)よりもさらに大きい規模で`packages/phrase-content/src/phrases.ts`に259件を追加し、321件→580件(初期の85件から約6.8倍)へ拡充した。
  - micro(単語): 100件→179件(体の部位「頭」「肩」「腕」「膝」、衣類「シャツ」「コート」「手袋」、家具・生活用品「椅子」「毛布」「茶碗」、乗り物・場所「バス」「病院」「学校」、時間語「朝」「週末」「時間」、文房具「はさみ」「定規」など79件追加)
  - short(短文): 70件→130件(新規の単語プールを使った短文60件を追加。「首を回す」「胸を張る」「椅子に座る」「バスに乗る」「新聞を読む」「シャツを着る」など)
  - standard(標準文): 93件→163件(短文と同じ語彙を組み合わせた標準文70件を追加。「椅子に深く座る」「バスの中で本を読む」「石鹸をよく泡立てる」など)
  - long(長文): 58件→108件(場面描写を含む長文50件を追加。「椅子に座って静かに本を読む」「病院で長い時間待たされて疲れた」「頭を使って難しい問題を解いていく」など)
  - `docs/CONTENT_GUIDELINES.md`の目標件数表を最新件数(micro 179 / short 130 / standard 163 / long 108)に更新。
  - 執筆時に1件、モーラ数がtierの帯域を外れるミス(`move_0033`「船で港を出発する」が standard の上限を超過)があったが、`npm test`の自動検証で検出し「船で港を離れる」に修正して解消した。以降`npm test`で全580件の検証(ID・表示文の重複なし、Tierとモーラ数の一致、読み仮名の入力可能性、表示文20文字以内)がパスすることを確認済み。tierRatio(D-044)は変更していない。
- **理由:** D-046の120件追加だけでは反復感の解消が不十分だった。単語(体の部位・衣類・生活用品・場所・時間・文房具)を新規に大きく増やし、その新しい単語を組み合わせて短文・標準文・長文を作ることで、語彙の重複だけでなく文章パターンの重複も減らした。
- **教訓:** 大量の文章を人力で追加する際は、モーラ数を事前に手計算するだけでなく`npm test`による自動検証を都度実行し、tier不一致のようなミスを早期に検出・修正するフローが有効(実際に1件検出・修正できた)。CONTENT_GUIDELINES.mdのMVP目標(short 100 / standard 300 / long 100)のうちshortは既に到達、standardは過半、longも過半に達したが、ユーザーが要求する「5倍」(約1600件)にはまだ届いておらず、自然で重複のない日本語コンテンツを人力で大量生産することには実務上の限界があるため、必要であれば複数回に分けて継続拡充する。

## D-048: ニックネーム変更機能とタイピング分析ページの追加

- **日付:** 2026-07-21
- **内容:** ユーザーから2点の要望を受けた。「ランキング登録用ニックネームを最初に聞いた後、変更できるようにしてほしい」「タイピング終了後に、平均ペース・ミスの割合・キーボード配置図での苦手キー分析ができる専用ページを作ってほしい」。
  - **①ニックネーム変更:** `apps/web/src/screens/ResultScreen.tsx`の`RankingSubmitBox`を変更した。
    - `loadNickname()`の結果を`useState`で保持するように変更(従来はマウント時の1回限りのスナップショットで、変更しても再レンダリングに反映されなかった)。
    - 保存済みニックネームがある状態の表示に「ニックネームを変更」ボタンを追加。押すと入力欄が現れ、`saveNickname()`で保存後、以降のランキング登録に反映される。
    - 変更は**今後の登録にのみ**反映され、既にこの試合で自動送信済みのランキングエントリ(サーバー側に匿名保存、更新APIなし)は過去の名前のまま残る。この制約は仕様として許容(スコアの所有者認証・更新APIを新設するのは今回のスコープ外)。
  - **②タイピング分析ページ:** これまで平均KPM・正確率の集計値はあったが、キー単位の分析データは存在しなかったため、新規にゼロから実装した。
    - `packages/game-core/src/player.ts`: `PlayerCore`に打鍵ログ(`keyLog: {key, correct, atMs}[]`)を追加し、`feedLocked`/`feedCandidates`の受理・拒否の両分岐で記録するよう計装。`getSummary()`から呼ばれる`computeAnalysis()`で、キーごとのミス率(`missRate`)・正解キー間の平均間隔(`avgIntervalMs`、直前の正解キーからの経過時間)を集計し、試行回数3回以上のキーに絞って「苦手なキー」上位8件(`weakKeys`)を抽出する。resolving(連鎖演出)中のfeedKeyは既存仕様どおり無視されるため、分析にもノイズとして乗らない。
    - `packages/game-core/src/types.ts`: `KeyStat`・`TypingAnalysis`を新設し、`PlayerSummary`に`analysis: TypingAnalysis`を追加(`SurvivalSummary`・`DuelSummary.player`/`.cpu`すべてに自動的に反映)。
    - `apps/web/src/screens/AnalysisScreen.tsx`(新規): 総打鍵数・正確率・ミス数・平均打鍵間隔のサマリーに加え、QWERTY配列を模したキーボード図を表示。各キーはミス率(70%)と打鍵の遅さ(30%)を合成した「苦手度」に応じて緑→黄→赤のヒートマップで色分けし、押されたことのないキーは灰色で区別する。苦手なキー上位をリストでも表示。
    - `apps/web/src/App.tsx`: `Screen`ユニオンに`"analysis"`を追加。結果画面から遷移した際は同じ結果画面へ戻れるよう遷移元を保持する。`ResultScreen.tsx`のサバイバル・対戦の両結果画面に「タイピング分析を見る」ボタンを追加(対戦は自分側`summary.player.analysis`のみ表示、CPU側は対象外)。
    - `packages/game-core/test/survival.test.ts`: 正解/ミスの記録・キーごとのミス率・平均間隔の計算・weakKeysの閾値フィルタを検証する3件のテストを追加。
    - 実ブラウザ(ローカルdevサーバー)で`window.__typeblastDebug`を用いて実際に打鍵ログを生成し、ResultScreen→AnalysisScreenの画面遷移とヒートマップの配色(ミスキーが正しく赤系に、正解キーが緑になること)を目視確認済み。
- **理由:** ①はユーザー体験上の明確な不便(誤字を直せない)の解消。②は「本気で」という要望に応え、単なる集計値の羅列ではなく、キーボード配置という直感的な視覚情報を使って「どこを練習すべきか」を具体的に示せるようにした。
- **教訓:** `resolving`中は`feedKey`が無視される既存仕様があるため、新しい計装(打鍵ログ)もその制御フローの内側(`feedLocked`/`feedCandidates`の中)に置くことで、無効な入力が分析データに混入しない設計を自然に維持できた。分析用の生ログ(`keyLog`)と集計(`computeAnalysis`)を分離したことで、将来的に異なる集計(区間別ペースの推移など)を追加する際もログ側の変更が不要になる見込み。

## D-049: タイピング分析ページに「次の改善につながる」項目を追加

- **日付:** 2026-07-21
- **内容:** D-048のタイピング分析ページ(総打鍵数・正確率・キーボードヒートマップ・苦手なキーTOP8)についてユーザーから「もっと他にも表示したほうが良いものあるんじゃない?しっかりと次の改善に繋がるような」との指摘を受けた。単なる集計値の羅列に留まらず、「次に何を練習すべきか」を具体的に示すため、4点を追加した。
  - **①前半/後半のペース比較:** `packages/game-core/src/player.ts`の`computeAnalysis()`で、打鍵ログを経過時間の中間点で二分し、それぞれの正確率・平均打鍵間隔(`PaceSegment`)を算出。疲れ・急ぎによる後半の崩れ(または逆に後半の調子上がり)を検出できるようにした。
  - **②手・指ごとのミス率:** タッチタイピングの標準的な運指表(`FINGER_OF_KEY`)を新設し、キーごとの統計を左右の手(`HandStat`)・8本の指(`FingerStat`)単位に再集計。個々のキーよりも上位の単位(「右手小指が苦手」など)で示すことで、より実践的な練習の焦点を示せるようにした。
  - **③直近の成績推移:** 既存の`StoredResult`履歴(`apps/web/src/storage.ts`、同じ難易度の直近10件)を`ResultScreen`経由で`AnalysisScreen`に渡し、直近5件の正確率を棒グラフで表示。継続的な練習で実際に上達しているかを確認できるようにした(DUELモードは対象外)。
  - **④「次に意識すること」の一言集約:** 苦手なキー→ペースの崩れ→指の偏り→全体的な正確率の順に優先度をつけ、最も重要な1つを選んで具体的な行動として文章化する`buildNextFocus()`を実装し、画面上部に強調表示した。
  - **バグ修正:** 実装後の目視確認で、試行回数がしきい値(3回)を超えているだけでミスが1回もないキーが「苦手なキーTOP」に紛れ込む不具合を発見した。`weakKeys`のフィルタ条件に`incorrect > 0`を追加して解消し、再発防止のテストを追加した。
  - `packages/game-core/src/types.ts`に`PaceSegment`・`HandStat`・`FingerStat`を追加し、`TypingAnalysis`を拡張。`packages/game-core/test/survival.test.ts`に手・指の集計、前半/後半の分割、ミスゼロのキーが除外されることを検証する計3件のテストを追加(全76テストに拡大)。
  - 実ブラウザ(ローカルdevサーバー)で`PlayerCore`の`keyLog`を直接操作して意図的に「後半にミスが増える・特定の指に偏る」データを作り、前半/後半カード・手/指のバー・「次に意識すること」の文言・修正後のweakKeysが正しく表示されることを目視確認した。
- **理由:** 個別キーの統計だけでは「結局何を練習すればいいか」が伝わりにくい。時間帯・手指という上位の切り口と、複数のシグナルから最重要ポイントを1つに絞る要約を加えることで、分析ページ単体で「次のアクション」が完結するようにした。
- **教訓:** 分析機能を作った直後の目視確認は、単体テストでは想定していなかったデータの組み合わせ(試行回数は十分だが誤り0件)を洗い出すのに有効だった。集計ロジックに閾値フィルタを設けるときは、閾値を満たすことと「本当にその特性を持つこと」(ここでは「ミスがある」)を混同しないよう、条件を分けて明示的にテストすることが重要。

## D-050: 連鎖エスカレーション演出(ズームパンチ・シェイク/フラッシュの連鎖比例強化)

- **日付:** 2026-07-22
- **内容:** 「ゲームフィール/爽快感」強化の3施策のうち1つ目。連鎖が深くなるほど演出が段階的に派手になるようにした。純粋に演出のみで、ゲームロジック(`packages/game-core`)には一切手を入れていない。
  - `apps/web/src/render/BoardRenderer.ts`: `blocksCleared`イベントのフラッシュ・シェイクの強度計算を、連鎖数(`event.chain`)に比例して滑らかに増える式に変更した(以前は連鎖3以上・5以上のしきい値でしか強くならなかった)。
  - 新規に「ズームパンチ」(`punchAmp`)を追加: 連鎖が2以上、またはTYPE BURST発動時に、盤面中央を軸に一瞬拡大してから素早く戻る演出。連鎖が深いほど拡大量が大きくなる(`Math.min(0.05 * chain, 0.34)`、上限あり)。指数減衰で毎フレーム縮小して自然に戻す(既存の`shakeAmp`と同じ減衰方式)。
  - `clearEffects()`(チュートリアルのステップ切り替え時に演出をリセットする既存メソッド)にも`punchAmp`のリセットを追加し、前のステップの余韻を持ち越さないようにした。
  - サウンド(`SoundEngine.chainStep()`)は既に連鎖数に応じてピッチが段階的に上がる実装(設計書§15)が入っており、変更不要だった。
- **理由:** 大きな連鎖ほど「ドカーン」と感じさせるには、既存の一部しきい値ベースの演出だけでは連鎖3→4→5…の間の変化が乏しかった。連鎖数に比例する連続的な式にすることで、どの深さでも「一段強くなった」ことが体感できるようにした。
- **教訓:** なし(既存の`shakeAmp`減衰パターンを`punchAmp`にもそのまま流用でき、実装は素直だった)。

## D-051: 大連鎖スローモー(5連鎖以上で拡張ヒットストップ+ズームイン)

- **日付:** 2026-07-22
- **内容:** 「ゲームフィール/爽快感」強化の2つ目。5連鎖以上に到達した際、実時間換算のsim time比自体は変えず(決定論を壊さないため)、既存の`config.chain.hitStopMs`の仕組みを使って「魅せる」一瞬の間を作った。
  - `packages/game-core/src/config.ts`: `chain`設定に`bigChainDepth`(5)・`bigChainHitStopMs`(260)・`bigChainHitStopStepMs`(40)・`bigChainHitStopMaxMs`(500)を追加。深さに応じて拡張ヒットストップが伸び、上限で頭打ちになる。
  - `packages/game-core/src/player.ts`: `stepResolving()`の自動連鎖判定部分で、新しい連鎖深度が`bigChainDepth`以上になった場合、即座にスコア加算・イベント発火を行わず、`stage: "hitstop"`へ遷移させて`bigChainHitStopMs(depth)`だけ待たせるようにした。待機後は既存の"hitstop"ステージ処理(スコア加算・`blocksCleared`発火・"clearing"への遷移)がそのまま流用されるため、ロジックの重複を避けられた。通常の連鎖(5未満)は従来通り即座に確定する。
  - `packages/game-core/src/types.ts`: `PlayerSnapshot`に`bigChainImpact: boolean`を追加(拡張ヒットストップの最中かどうか)。
  - `apps/web/src/render/BoardRenderer.ts`: `snapshot.bigChainImpact`が真の間、盤面をわずかにズームインしたまま維持し(既存のズームパンチ`punchAmp`に加算)、金色の縁が絞られるビネット(`drawBigChainVignette`)を表示する。
  - `packages/game-core/test/survival.test.ts`: `bigChainHitStopMs()`の深さ別の値・上限頭打ちの検証、および実際に5連鎖目に入った際に拡張ヒットストップの間は`blocksCleared`が発火せず、時間経過後に発火することを検証するテストを追加(既存の`resolving`状態を直接差し替える手法を使用)。
  - ブラウザ(ローカルdevサーバー、`window.__typeblastDebug`)で実際に5連鎖以上のシナリオを`core.resolving`を直接差し替えて再現し、`bigChainImpact`が正しくtrue→falseに遷移することを確認済み。
- **理由:** ユーザー指示により、シミュレーションの実時間比を変えるスローモーは決定論(`survival.test.ts`の「同じSeedと同じ操作列から同じ結果になる」テスト)を壊すリスクがあるため避け、既存のヒットストップの仕組みを流用する設計とした。
- **教訓:** 既存の"hitstop"ステージ処理が「resolving状態のどのフィールドが設定されていても同じように処理する」設計になっていたため、新しい連鎖ステップからそのステージへ分岐させるだけで、スコア加算・イベント発火のロジックを一切複製せずに済んだ。既存の状態機械が十分に汎用的だったことが素直な拡張につながった。

## D-052: フィーバータイム(6連鎖以上でスコア2倍・数秒間の演出)

- **日付:** 2026-07-22
- **内容:** 「ゲームフィール/爽快感」強化の3つ目。6連鎖以上を達成すると8秒間スコア2倍になる「FEVER」状態を追加した。ユーザーからの明確な指示により、ループBGMは一切追加せず、単発SFX+画面演出+スコア倍率のみで表現している。
  - `packages/game-core/src/config.ts`: `fever: { triggerChainDepth: 6, durationMs: 8000, scoreMultiplier: 2 }`を追加。
  - `packages/game-core/src/types.ts`: `GameEvent`に`feverStarted`/`feverEnded`を追加。`PlayerSnapshot`に`feverActive: boolean`・`feverMsLeft: number`を追加。
  - `packages/game-core/src/player.ts`: `feverMsLeft`を追加し、`advance()`内で経過時間ぶん減算(0になったら`feverEnded`を発火)。連鎖確定時(`stepResolving`の連鎖終了処理)に深度が`triggerChainDepth`以上なら`triggerFever()`を呼び、未発動なら`feverStarted`を発火しつつ`feverMsLeft`を`durationMs`にリセットする(既に発動中なら無音でリセット=延長)。スコア加算箇所(正解キー・PERFECTボーナス・ブロック消去・連鎖ボーナス・TYPE BURST発動・全消しボーナス)を`applyFeverMultiplier()`経由に統一し、フィーバー中は`scoreMultiplier`倍になるようにした。ゲージ増加量はフィーバーの影響を受けない(スコアのみを対象とする設計)。チュートリアルのステップ切り替え(`loadTutorialBoard`)でも`feverMsLeft`をリセットし、次のステップに持ち越さないようにした。
  - `apps/web/src/audio/SoundEngine.ts`: `feverStart()`・`feverEnd()`を追加(いずれも単発のシンセSFX。ループ音源は一切使用していない)。
  - `apps/web/src/game/GameController.ts`: `feverStarted`→`sound.feverStart()`、`feverEnded`→`sound.feverEnd()`を配線。
  - `apps/web/src/render/BoardRenderer.ts`: `feverStarted`/`feverEnded`イベントで「FEVER!!」/「FEVER END」のポップアップとフラッシュを表示。`snapshot.feverActive`の間、盤面全体にごく薄い暖色ティント(`drawFeverOverlay`)と、バーストより太く速いパルスの外周ボーダー(金↔赤の明滅)を表示し続ける。
  - `apps/web/src/screens/GameScreen.tsx`: HUDに「🔥 FEVER TIME! SCORE ×2 (残り秒数)」の予約領域バッジを追加(D-031のパターンに従い、常にDOM上に領域を確保し`visibility`で切り替えることでレイアウトのガタつきを防ぐ)。
  - `apps/web/src/styles.css`: `.fever-badge`を追加(既存の`.streak-badge`/`.danger-badge`と同系統のスタイル)。
  - `packages/game-core/test/survival.test.ts`: フィーバーのスコア倍率適用・6連鎖到達時の自動発動・残り時間0での自動終了(`feverEnded`発火)を検証するテストを追加。
  - ブラウザ(ローカルdevサーバー)で実際に6連鎖シナリオを`core.resolving`の直接差し替えで再現し、スコアが期待通り(通常のブロック消去分400 + 連鎖ボーナス18000 + フィーバー中に確定した全消しボーナス10000<フィーバー発動と同じ連鎖の中で全消しボーナスにも2倍が適用される> = 28,400)になること、HUDに「🔥 FEVER TIME! SCORE ×2」バッジが表示されること、8秒経過後に`feverActive`がfalseへ戻ることを確認済み。
- **理由:** 大連鎖という「達成」に対して、その場限りの演出だけでなくスコアという実利のある報酬を数秒間持続させることで、プレイヤーがより深い連鎖を積極的に狙う動機を作る狙い。BGM復活はユーザーが過去に明確に拒否した経緯があるため、単発SFXと画面演出のみに徹した。
- **教訓:** フィーバー発動判定を連鎖確定処理の中(スコア加算の直後)に置いたことで、フィーバーを発動させた連鎖そのものの全消しボーナスにも倍率がかかるという、狙って作ったわけではない「同じ連鎖でボーナスが二重に効く」派手な挙動が生まれた。派手さの面では良い効果だが、今後スコア計算の順序を変える際はこの副作用(発動タイミングと適用範囲の順序依存)を意識する必要がある。

## D-053: 語彙テーマ選択・季節おすすめ表示・タイトル画面ファーストビュー改善

- **日付:** 2026-07-22
- **内容:** TikTok経由の新規流入を見据えた「飽きさせない/第一印象を強くする」施策として、3点を実装した。
  - **① 語彙テーマ選択:** ランディング画面で出題ジャンルを「おまかせ / ビジネス / 季節・イベント」から選べるようにした。
    - `packages/phrase-content/src/themes.ts`(新規): `VOCAB_THEMES`定義と`buildThemedPhrasePool(themeId, all)`。テーマは`category`によるフィルタで表現する。純粋関数(静的配列へのフィルタのみ)なのでgame-coreのシミュレーション決定性には一切影響しない。
    - **プール枯渇対策(最重要):** 難易度はtier単位で出題し、特に初級はmicro(単語)が約8割を占める。一方`PlayerCore`のコンストラクタは有効フレーズ数が10未満だと例外を投げる。そこで`buildThemedPhrasePool`は**tierごとに**判定し、そのテーマのそのtierが`MIN_PHRASES_PER_TIER`(8件)未満のときだけ、そのtierに限りグローバルプールへフォールバックする設計にした。これにより「テーマを選んだら初級がクラッシュする/同じ単語ばかり出る」事故を構造的に防ぎつつ、劣化を該当tierのみに限定できる。
    - 実際にはビジネス(仕事)・季節ともに全tierへ語彙を追加したためフォールバックは発生しない(仕事85件・季節95件、各tier20件以上)。語彙総数は580件→676件。
    - `apps/web/src/storage.ts`: `Settings.theme`を追加し選択を永続化(既定`"all"`で従来挙動と完全に同じ)。`GameController`の`GameMode`に`theme?`を追加し、サバイバル生成時にテーマ別プールを渡す。
    - **CPU対戦はテーマ非継承(常におまかせ)** とした。プレイヤーとCPUで出題プールが変わると対戦バランスの検証が複雑になるため、単純さを優先した。
  - **② 季節テーマの「今月のおすすめ」表示:** `apps/web/src/seasonal.ts`(新規)で現在の月から季節絵文字(🌸春/🎆夏/🍁秋/❄️冬)を返し、季節テーマのチップにバッジ表示する。**コンテンツを日付でロックすることは一切しない**(いつでも全テーマを選べる)。純粋なUI演出。
  - **③ タイトル画面ファーストビュー改善:** 従来は5ステップの「遊び方」リストが画面上部を占有し、主要CTAが埋もれていた。ヒーロー(ロゴ+タグライン、`reducedMotion`時は無効化されるCSSのみのブロック落下アニメ付き)を最上部に置き、サバイバル開始ボタン+難易度+テーマ選択をその直下へ。「遊び方」は折りたたみ式(既定で閉)に変更し、チュートリアル/ランキングへの導線は横並びのクイックリンクに集約した。既存機能はすべて維持。
  - `packages/phrase-content/test/content.test.ts`: 「全テーマ×全tierで健全なプールサイズを持つ(例外を投げない)」ことを含む語彙テーマのテストを追加。`docs/CONTENT_GUIDELINES.md`にテーマ表と追加時の必須条件を追記。
  - 検証: ローカルdevサーバーで「ビジネス×上級」→契約を結ぶ/名刺を渡す/予算を組む等、「季節・イベント×初級」→梅雨/初雪/立春/門松/七夕等が出題され、36ブロック全て重複なしであること、コンソールエラーが無いことを確認済み。
- **理由:** 語彙の反復感は繰り返しプレイ最大の飽き要因であり、テーマ選択は「推しジャンルで遊ぶ」動機付けとSNSでの話題化の両方に効く。タイトル画面は新規流入の第一接触点なので、情報量よりも「面白そう」と「すぐ始められる」を優先した。
- **教訓:** テーマ(カテゴリ)による絞り込みと難易度(tier)による出題は直交する軸であり、素朴に`filter`すると特定の組み合わせでプールが枯渇してクラッシュしうる。**交差する2軸で絞り込む場合は、片方の軸ごとに最小件数を保証しフォールバックする設計**が必要になる。今回はtier単位のフォールバックとテストによる全組み合わせ検証で担保した。

## D-054: 称号/ランクシステムと生涯累計プレイ統計の導入

- **日付:** 2026-07-22
- **内容:** 「進行度・継続率」強化として3施策(D-054〜D-056)を実装する最初の土台。累計プレイに応じて称号(タイピング見習い→…→バースト王)が上がるランキングシステムを追加した。
  - **ストレージの追加:** `apps/web/src/storage.ts`は従来、直近10件の`typeblast.results.v2`と設定・対戦成績・ニックネームしか持たず、生涯累計(総プレイ回数・累計スコア等)を保持していなかった。新規に`typeblast.progress.v1`キーを追加し、`LifetimeProgress`(総プレイ回数・累計スコア・自己ベストスコア/KPM/正確率・総文章数・総プレイ時間・最大連鎖)を保持する。既存の4キー(settings/results/duel/nickname)とは完全に独立した別名前空間なので、既存ユーザーのデータには一切触れない。
  - **称号ラダー:** 累計スコア(サバイバル・対戦を合わせた生涯獲得スコア)を単一の指標として10段階のラダーを定義した(0 / 5,000 / 20,000 / 50,000 / 100,000 / 200,000 / 400,000 / 700,000 / 1,200,000 / 2,000,000)。難易度別の比較(D-032)を持ち込まずに済み、プレイを続ける限り単調に増加するため「あと◯◯で次の称号」が常に成立する。
  - **新規ワークスペースパッケージ `packages/progression`:** 称号ラダー(`titles.ts`)・生涯統計のマージ/加算(`progress.ts`)・盤面カラーテーマの定義(`boardThemes.ts`、D-055)を持つ、localStorageに依存しない純粋なロジックのパッケージとして新設した。**本来はapps/web内に置く方が素直だが、このセッション開始時点で別セッションがルートの`package.json`/`tsconfig.json`/`vitest.config.ts`に未コミットの変更を残しており、それらは絶対にコミットしてはいけない制約があった。`vitest.config.ts`の`test.include`は`apps/web`を含んでいないため、apps/web内に置くと新しい単体テストが実行されない(サイレントに無視される)。一方`packages/*/test/**/*.test.ts`は既存の設定のまま新規パッケージにもマッチするため、設定ファイルを一切変更せずに済むこの構成を選んだ。** `apps/web`からは`@type-burst/progression`として利用し、実際のlocalStorage I/Oは引き続き`apps/web/src/storage.ts`が担う。
  - `apps/web/src/storage.ts`: `appendResult`(サバイバル)・`recordDuel`(対戦、プレイヤー自身の統計のみ)の両方から`recordProgress()`を呼び、`accumulateProgress()`(純粋関数)で生涯累計を更新する。あわせて、直近結果の保持件数を10件→60件に拡大した(D-054, Feature 3の成長グラフのため)。
  - **表示:** `LandingScreen`に称号バッジ+進捗バー(押すと成長記録画面へ)、`ResultScreen`に小さな称号バッジを追加。
  - `packages/progression/test/titles.test.ts`・`progress.test.ts`で、ラダーのしきい値・進捗計算・**保存データが存在しない/壊れている/一部フィールドのみの後方互換シナリオ**・累計加算の純粋性を検証(計16テスト)。
  - ブラウザ(ローカルdevサーバー)で、新規プレイヤー(localStorage空)・生涯スコア150万の想定ベテランプレイヤー・設定/結果は保存されているがprogressキーが存在しない旧バージョンのデータ、の3パターンをlocalStorageに直接投入して動作確認し、いずれもコンソールエラーなく正しい称号・残り必要スコアが表示されることを確認済み。
- **理由:** 継続プレイのモチベーションには「今どれくらい育っているか」を可視化する指標が有効。既存の直近10件だけの保存では生涯累計が失われるため、専用の永続化層が必須だった。
- **教訓:** リポジトリに他セッションの未コミット変更が同居している状況では、「コミットしてはいけないファイル」に依存するテスト基盤(この場合はvitest.config.tsのinclude glob)を触らずに単体テストを追加する必要がある。既存globの構造(`packages/*/test/**`)を理解した上で、新規の小さなワークスペースパッケージとして切り出すことで、設定ファイルを一切変更せずにテスト可能な構成を実現できた。

## D-055: 盤面カラーテーマ(アンロック要素)

- **日付:** 2026-07-22
- **内容:** 「アンロック要素」として、ユーザー案の「キーボードスキン/エフェクト色/背景テーマ」のうち、盤面のカラーテーマ1軸に絞って実装した(3軸を中途半端に実装するより、1軸を完成させる方針)。
  - `packages/progression/src/boardThemes.ts`: `BOARD_THEMES`(デフォルト/夕焼け/深海/ネオン/ロイヤルの5種)を定義。各テーマは4属性(fire/water/wind/light)の配色(fill/bright/dark)を持つ純粋なデータ。解放条件はD-054と同じ累計スコア指標を再利用し、称号のしきい値(20,000/100,000/400,000/1,200,000)と意図的に一致させ、「この称号でこの配色も解放」という分かりやすい連動を作った。
  - `resolveBoardTheme(themeId, totalScore, highContrast)`: 実際に描画へ適用するテーマを解決する関数。**未解放のテーマが指定された場合、およびHigh Contrast設定が有効な場合は、常にdefault配色へフォールバックする。** 特に後者は設計判断: カスタムテーマの配色によっては白文字との十分なコントラストを保証できないため、視認性を最優先してHigh Contrast中は問答無用でdefaultに固定した(要レビュー: ユーザーの好みによっては「High Contrastでも選んだ配色を尊重してほしい」という要望があり得るが、今回は安全側に倒した)。
  - `apps/web/src/render/BoardRenderer.ts`: 属性ごとの「形状」(三角/丸/菱形/星、ハイコントラスト等でも色に依らず区別できる既存の仕組み)と「配色」を分離した。配色は`attributeColors`という公開フィールドで外部から設定できるようにし、BoardRenderer自体は進捗・アンロックの概念を一切知らない(呼び出し側が解決済みの配色を渡すだけ)。ゲームロジックには一切影響しない見た目だけの変更。
  - `apps/web/src/game/GameController.ts`・`GameScreen.tsx`・`App.tsx`: `App`が`resolveBoardTheme(settings.boardTheme, progress.totalScore, settings.highContrast)`で解決した配色を`GameScreen`→`GameController`→`BoardRenderer`(メイン盤面・DUELのCPUミニ盤面の両方)へ伝搬する。
  - `apps/web/src/storage.ts`: `Settings.boardTheme`を追加(既定`"default"`で従来通りの配色)。
  - `LandingScreen`: 盤面カラーの選択チップを追加。未解放のテーマは🔒アイコン+`title`属性で解放条件(必要な累計スコア)を表示し、クリックできない(disabled)。High Contrast有効時はテーマ選択自体に「(High Contrast中は無効)」の注記を出す。
  - `packages/progression/test/boardThemes.test.ts`: 解放判定・フォールバック(未解放時・High Contrast時)を検証(10テスト)。
  - ブラウザで、生涯スコア150万のベテランプレイヤーが「ネオン」テーマを選択してサバイバルを開始した際、Canvas上のブロック実ピクセル色がデフォルト配色(fire=#c2402f等)ではなくネオン配色(fire fill=#c21f6b等)になっていることを`getImageData`で確認。さらにHigh Contrastを有効にした状態で同様にプレイし、「ネオン」選択中でもピクセル色がデフォルト配色に戻ることを確認済み(フォールバックが実際に効いている)。
- **理由:** 「見た目のカスタマイズを解放していく」という達成感を、ゲームバランスに一切影響しない形で提供する。既に属性ごとに形状(三角/丸/菱形/星)で区別する設計があったため、色だけを差し替える設計は既存のアクセシビリティ配慮(色だけに頼らない識別)と自然に両立できた。
- **教訓:** アンロック要素は「進捗指標をどこに置くか」が肝心で、称号と全く別の指標を導入すると「称号は上がったのに新しい色は増えない」という乖離が起き得る。同じ累計スコアを共通指標として再利用したことで、進捗の一貫性を保てた。

## D-056: 成長グラフ(長期的なKPM・正確率・スコアの推移)

- **日付:** 2026-07-22
- **内容:** `apps/web/src/screens/AnalysisScreen.tsx`(D-048/D-049)の「直近5件の正確率だけの棒グラフ」を、KPM・正確率・スコアの3指標を長期(最大30件)で見せる折れ線グラフに拡張した。
  - `apps/web/src/storage.ts`: 直近結果の保持件数を10件→60件に拡大(`MAX_STORED_RESULTS`、D-054で実施済み)。1件あたり数十バイトなのでlocalStorage容量上の懸念はない。
  - `AnalysisScreen.tsx`: `analysis: TypingAnalysis`を`TypingAnalysis | null`に変更し、**結果画面を経由しない場合(タイトル画面の「📈 成長記録」から)でも開けるようにした。** `analysis`が`null`のときはキーボードヒートマップ・苦手キー・前半後半比較・手指分析などプレイ単位のセクションを省略し、称号/生涯累計成績と成長グラフだけを表示する。既存の結果画面→分析画面の遷移(`analysis`がnull以外)は完全に従来通り。
  - 新規`GrowthChart`コンポーネント: 外部グラフライブラリを使わず、素の`<svg><polyline>`で折れ線グラフを描画(CSPが外部ホストをブロックするため、既存の手書きチャート方針を踏襲)。スコア・KPM・正確率の3枚を、直近30件を古い→新しい順に並べて表示。
  - `App.tsx`: `Screen`の`analysis`状態に`back: {name:"landing"} | ResultScreenState`を持たせ、結果画面経由・タイトル画面経由のどちらからも同じ画面を再利用しつつ、正しい戻り先に戻れるようにした。
  - ブラウザで、40件の疑似プレイ履歴(スコア・KPM・正確率が右肩上がりになるデータ)をlocalStorageへ投入し、3枚の折れ線グラフが正しい値(最新値・最高値)で表示されること、2件未満のときは「プレイを重ねると…」という案内文が出ること、タイトル画面の「成長記録」から開いた場合はキー分析セクションが表示されず「プレイすると…も表示されます」という案内が出ることを確認済み。
- **理由:** 10件では上達の傾向を見るには短すぎ、また既存の分析画面は「その場のプレイ結果」に強く紐づいていたため、結果画面を経ない長期的な振り返りの入口がなかった。
- **教訓:** 既存コンポーネントのpropsをoptional化(`TypingAnalysis | null`)して「プレイ単位のデータがない」状態を型で表現することで、新しい画面を作らずに済み、称号・成長グラフの表示ロジックを1箇所に集約できた。

## D-057: モバイル流入向けランディングページの追加(非対応の壁を撤去)

- **日付:** 2026-07-22
- **内容:** TikTokからの流入はほぼ全てモバイルである一方、`SmallScreenGuard.tsx`(D-022)は1024px未満の全訪問者に「非対応」の案内文だけを表示して弾いていた。ゲームを遊べるようにするのではなく(物理キーボード前提は変えない、モバイル最適化は引き続きMVP外)、離脱そのものを防いでPCでの再訪を後押しする専用ランディングページに差し替えた。
  - **アトラクトモード(自動プレイのデモ盤面):** 動画・GIF等のアセットファイルは追加せず(CSPで外部ホストを使えない/バンドルを軽く保つ)、`packages/game-core`の実際のゲームエンジン(`PlayerCore`)をそのまま「見せる」ために動かす方式にした。`packages/game-core/src/duel.ts`のCPU対戦用`CpuDriver`(D-014)を土台に、web層専用の`apps/web/src/game/attractDemoDriver.ts`(`AttractDemoDriver`)を新設した。game-core自体は一切変更せず、既存の公開API(`PlayerCore`・`findGroup`・`Prng`)だけを使っており、game-coreの決定論(Seed駆動・`Math.random`不使用)はそのまま維持される。表示は`apps/web/src/components/AttractBoard.tsx`が担い、盤面があふれたら固定シード配列(`SEEDS`、乱数選択はしない)を巡回して次のシードで作り直し、無限ループさせる。
  - **描画:** `BoardRenderer.ts`に`ATTRACT_RENDERER_OPTIONS`(cellW48/cellH38/pad8、`drawText: true`・`overlays: false`)を追加。既存の`MAIN_RENDERER_OPTIONS`(PC盤面)・`MINI_RENDERER_OPTIONS`(CPU対戦の相手盤面)とは完全に別の定数で、両者には一切手を入れていない。
  - **性能・アクセシビリティ配慮:** `prefers-reduced-motion`(`matchMedia`で監視)が有効な場合はループを回さず初期盤面を1枚だけ描画する静止画にフォールバックする。`document.visibilitychange`でタブが非表示になったら`requestAnimationFrame`を`cancelAnimationFrame`で完全停止し、復帰時に再開する。フレーム処理は概ね30fpsへ間引き(低スペック端末対策)。音声は一切再生しない(元々`PlayerCore`自体がサウンドを持たず、`SoundEngine`を接続していないため自動的に無音)。
  - **CTA:** 「URLをコピー」(`navigator.clipboard.writeText`、失敗時は`document.execCommand("copy")`によるレガシーフォールバック、成功/失敗をボタンラベルで2.2秒間表示)と、「自分に送る(LINEなど)」(`navigator.share`、`typeof navigator.share === "function"`でfeature-detectし、非対応環境ではボタン自体を出さない)を実装。コピー対象は`window.location.href`(TikTok広告等のUTMパラメータが付いていてもそのまま維持される)。
  - **世界ランキング(おまけ):** 「安く済むなら」という条件だったため、PC版`RankingScreen.tsx`(`useFitToViewport`前提のフル画面UI)を流用せず、`apps/web/src/components/MobileRankingPreview.tsx`という難易度固定(中級)・上位10件のみの軽量版を新設し、折りたたみの奥に置いた。
  - `SmallScreenGuard.tsx`: 1024px未満の分岐だけを`MobileLanding`に差し替えた。**1024px以上の分岐(`return children`)は1文字も変更していない。** 従来、狭い画面では`AppRoot.tsx`の子要素(`AdSlots`/`App`/`ConsentBanner`/`Analytics`)が丸ごとレンダリングされていなかったため、モバイル流入の計測ができていなかった。広告・同意バナーはモバイルファネルの主目的(離脱防止)を阻害するので出さないが、Cookie不使用・常時計測の`<Analytics />`(D-037)だけはモバイル側の分岐にも追加し、TikTok経由の到達数を計測できるようにした。
  - 従来の`.narrow-guard*` CSSは参照元(`SmallScreenGuard.tsx`の旧マークアップ)ごと削除し、新規クラスは全て`.mobile-landing-`接頭辞に統一してPC側の既存クラス(`.hero`/`.logo`等)と一切共有しないようにした。
  - 検証: ローカルdevサーバーで375×812(モバイル)と1400×900(PC)の両方を確認。PC側は称号バッジ・サバイバル/対戦・設定行など従来通り全て表示されコンソールエラーなし。モバイル側は新ランディングの全文言・ボタンが表示され、`AttractDemoDriver`+`PlayerCore`+`BoardRenderer`を手動で400フレーム(20秒相当)進めるスクリプトで9回のブロック消去・4回の連鎖・1回のTYPE BURST発動とスコア加算、Canvas全ピクセルへの描画を確認した。コピー/共有ボタンはクリックディスパッチで動作確認(このサンドボックス環境は`navigator.clipboard`の書き込み権限が常に拒否されるため、成功時の「コピーしました!」ではなく失敗時のフォールバック表示「コピーできませんでした」→2.2秒後に自動で「URLをコピー」へ戻る、という失敗経路の方を実機として確認した形になる。実ブラウザでの成功パスは未検証)。
- **理由:** TikTok施策の効果はモバイル訪問者をどれだけ後日PCでのプレイに転換できるかにかかっており、従来の「非対応」の壁はこの転換を構造的に阻害していた。動画アセットを追加せず実エンジンをそのまま動かす方式にしたことで、マーケティング用に別途「盤面が動いているように見えるだけの偽物」を作るコストと、実物とのズレ(スクリーンショット詐欺のリスク)の両方を避けられた。
- **教訓・要レビュー:** ①このサンドボックス環境ではタブの`document.hidden`が常に`true`(自動テスト特有の挙動、D-020と同種の制約)で、`requestAnimationFrame`ベースの本番相当ループを実機のように継続実行させて確認することができなかった。ロジック自体(ドライバ→`PlayerCore`→`BoardRenderer`)は上記の手動フレーム送り検証で正しいことを確認済みだが、実際のモバイル実機での見た目の滑らかさは未検証のため、公開後に実機で一度確認してほしい。②同様の理由で`navigator.clipboard.writeText`の成功パス(「コピーしました!」表示)も実ブラウザでの動作は未検証。③アトラクトモードの「見栄えの良さ」(どのシードが最も連鎖やTYPE BURSTを頻繁に見せるか)はチューニングの余地があり、`SEEDS`配列(`attractDemoDriver.ts`と対になる`AttractBoard.tsx`)の差し替えだけで調整可能な設計にしてある。

## D-058: アトラクトデモが60Hz以上の端末で永久に描画されないバグを修正

- **日付:** 2026-07-22
- **内容:** D-057で追加したモバイル・ランディングのアトラクトデモ(`apps/web/src/components/AttractBoard.tsx`)に、実機では致命的だが検証環境では発覚しないバグがあったため修正した。
  - **バグ:** 30fpsへ間引く処理が `const dt = now - last; last = now; if (dt < MAX_FRAME_MS) return;` の順になっており、**描画をスキップする場合にも `last` を更新していた**。このため `dt` は常に1フレーム間隔(60Hzなら約16.7ms)にしかならず、閾値の33.3ms(=1000/30)を永久に超えられない。結果、`renderer.draw()` が**一度も呼ばれず**、「実際のゲーム画面(自動プレイ中)」とラベルの付いた枠が空白のまま表示される。ロジックをシミュレートしたところ、毎秒の描画回数は 60Hz:0回 / 120Hz:0回 / 30Hz:19回 だった(**現代のスマホのほぼ全てが該当する60Hz以上で完全に破綻**、たまたま30Hz相当でのみ動く)。
  - **修正1:** `last = now` を早期リターンの**後**へ移動し、描画をスキップした分の時間を累積させるようにした。修正後の描画回数は 60Hz:22回 / 120Hz:27回 / 30Hz:20回 と、いずれも意図通り約30fpsに収まる。
  - **修正2:** RAFの初回発火を待たずに、マウント直後へ初期盤面の同期描画を1回追加した。RAFが抑制される環境や起動直後の一瞬でも枠が空白にならないようにするための保険。
  - **なぜD-057の検証で見逃されたか:** このサンドボックスのヘッドレスブラウザでは `requestAnimationFrame` が全く発火しない(実測で1秒あたり0回)ため、そもそもこのループを実行できなかった。D-057では代わりにドライバとコアを手動で400フレーム進めて描画パスの健全性を確認しており、**ループ自体の時間管理ロジックだけが未検証のまま残っていた**。今回、本番環境のcanvasのピクセルを直接サンプリングして「非透明ピクセル0・色数1(=完全な空白)」を検出したことで発覚した。
  - 修正後、ローカルで同じピクセル検査を行い 非透明4,965px・863色(盤面が完全に描画されている)を確認。PC表示(1400x900)が従来通りであること、コンソールエラーが無いことも確認済み。
- **理由:** アトラクトデモはモバイル流入の第一印象そのものであり、空白の枠が表示されると「壊れているサイト」という最悪の印象を与える。TikTok誘導を始める前に潰す必要があった。
- **教訓:** **「間引き処理では、スキップ時にタイムスタンプを更新してはいけない」**(更新すると時間が累積せず閾値に到達しない)。加えて、検証環境が特定のブラウザAPI(ここではRAF)を提供しない場合、そのAPIに依存するコードは"未検証"のまま通過してしまう。**最終成果物のピクセル(あるいは実際の出力)を直接検査する**ことで、実行経路をトレースできない環境でも破綻を検出できた。サブエージェントに実装を委譲する場合も、成果物そのものの独立検証は省略すべきでない。

## D-059: 語彙テーマ・盤面カラーの撤去、分析画面レイアウト修正、サバイバル無限プレイの再修正

- **日付:** 2026-07-22
- **内容:** ユーザーから4件の指摘を受け、以下を実施した。
  1. **語彙テーマ選択(D-053)を撤去。** 「ランディング画面でおまかせ/ビジネス/季節・イベントを選ぶ」UIとフィルタ機構(`packages/phrase-content/src/themes.ts`の`VOCAB_THEMES`/`buildThemedPhrasePool`、`apps/web/src/seasonal.ts`の季節バッジ)を削除し、`GameController`は常に全カテゴリの`PHRASES`を使う(D-053以前の挙動)に戻した。**「仕事」「季節」カテゴリの語彙そのもの(D-042/D-047/D-053で追加した単語)は削除しておらず**、全カテゴリ共通プールの一部として引き続き出題対象になる。`Settings.theme`フィールド、関連テスト(`content.test.ts`・`survival.test.ts`の「語彙テーマ」describe)、`docs/CONTENT_GUIDELINES.md`の該当節も削除した。
  2. **盤面カラーテーマ(D-055)を撤去。** `packages/progression/src/boardThemes.ts`(5色のアンロック要素)とそのテストを削除し、`packages/progression/src/index.ts`から`export * from "./boardThemes"`を除去。`App.tsx`の`resolveBoardTheme`呼び出し、`GameScreen`/`GameController`/`BoardRenderer`の`attributeColors`props threadingを撤去し、`BoardRenderer.ts`はD-055以前の`ATTRIBUTE_STYLES`(静的定数、色+形状を一体で保持)へ戻した。**称号システム(D-054、`titles.ts`)と生涯累計統計(`progress.ts`)は同じ`packages/progression`パッケージ内だが無関係のため一切変更していない。**
  3. **タイピング分析画面(`AnalysisScreen.tsx`)のレイアウト崩れを修正。** 添付画像で「苦手なキー TOP8」グリッドの行高がバラつき、ジグザグに見える崩れが指摘された。`.weak-keys-list`を`flex-wrap`(内容量依存のいわゆる段落折り返し的レイアウトで、カード幅が不揃いだと縦横の整列が保証されない)から`display:grid; grid-template-columns: repeat(2, 1fr)`固定2列へ変更し、各カードに`min-height`を設定して行の高さを揃えた。あわせて`.finger-label`(指ラベル)に`white-space:nowrap`と幅の余裕を追加し、指名のラベル長に関わらずミス率バーの開始位置が常に揃うようにした。`.screen.analysis`の要素間`gap`も16px→22pxへ広げ、セクション同士の余白を他画面と揃えた。ブラウザで擬似プレイデータ(30文章・タイプミスあり)を生成し`getBoundingClientRect()`で実際の座標を検証、修正後は2列グリッドの各行が同一x座標に揃い、指ミス率バーの開始x座標も全指で一致することを確認した(このサンドボックス環境はスクリーンショット撮影が機能しないため、座標の直接検証で代替した)。
  4. **サバイバルの行上昇floor(`survivalRise.minIntervalMs`)を再修正。** D-041で2400ms→1200msへ引き下げたが、micro tier中心のeasy(初級)では1200msでも頭打ち後を事実上いつまでも捌けてしまい「初級だと無限に生存できる」状態が再発していた。「一定値で頭打ちする」設計自体をやめ、floorを技術的な下限(0除算・ビジー無限ループを避けるためだけの値、50ms)まで引き下げることで、頭打ち以降も実質的に誰にも捌けないペースまで際限なく加速し続けるようにした。floorは全難易度共通の`DEFAULT_CONFIG.survivalRise`のままなので、「難易度間で行上昇速度を変えない」というユーザーの既存指示には抵触しない。`PlayerCore.advanceRise()`の`while (this.riseTimerMs <= 0 && !this.toppedOut)`ループはfloorが50msでも各回`riseTimerMs`が正の値だけ増加するため無限ループの心配はない(dtは1フレームあたり最大100ms/50msにクランプ済み)。
  - 検証: `npm run typecheck && npm test && npm run build`(97 tests)全て通過。ブラウザでランディング画面から語彙テーマ・盤面カラーのUIが完全に消えていること、サバイバル→分析画面まで実プレイで到達しコンソールエラーが無いことを確認済み。
- **理由:** ユーザーから明示的に「語彙テーマ・盤面カラーは不要」「分析画面のバランスが悪い」「初級が無限にできてしまう」との指摘を受けた。
- **教訓:** 一度実装した機能でも、ユーザー自身がその場で確認して「要らない」と判断した場合は、根拠となった元コミット(D-053/D-055)を参照して**その機能追加分だけを正確に巻き戻す**(無関係な称号・成長グラフ等の周辺機能は触らない)のが、独自の削除ロジックを新たに書くより安全で速い。また、このサンドボックス環境はRAFだけでなくスクリーンショット撮影自体も機能しないため、レイアウト検証は`getBoundingClientRect()`による実座標の直接検証で代替する必要がある(D-058で判明したRAF未対応と同種の環境制約)。

## D-060: キーボードショートカット競合とランキング結果再送信の修正

- **日付:** 2026-07-22
- **内容:** 実際のキーボード操作を含む通し確認で、画面単位のグローバルショートカットと通常のUI操作が競合する問題、およびランキング送信の不整合を修正した。
  1. タイトル画面のEnter/Spaceによるサバイバル開始は、ボタン・チェックボックス・リンク等の操作中には発動しないようにした。修正前はHigh ContrastチェックボックスへフォーカスしてSpaceを押しただけでサバイバルが開始し、チェック自体も切り替えられなかった。
  2. 結果画面のEnter/Spaceによる再戦も同様に、ボタンや入力欄の操作中には発動しないようにした。「タイピング分析を見る」「タイトルへ」等をキーボードで押した際に再戦処理と競合する経路を解消した。入力中のEscも従来どおり画面遷移に使わない。
  3. ランキングAPIが拒否する0点の結果では登録フォーム・自動送信を出さず、登録条件を案内する表示へ変更した。
  4. 送信成功済みの試合Seedを`sessionStorage`へ記録し、分析画面から同じ結果画面へ戻ってコンポーネントが再マウントされても、同じ試合結果を自動再送信しないようにした。保存不能な環境でもゲームや初回送信は阻害しない。
- **理由:** マウス操作だけでは見えないが、アクセシビリティ上必須としていた標準HTML要素のキーボード操作をグローバルショートカットが横取りしていた。また、結果画面の再マウントを新しい試合と誤認して同一スコアを再送信する設計は、ランキングに重複記録を作り得た。
- **教訓:** 画面全体のEnter/Spaceショートカットは、フォーカス先が操作可能要素かを必ず確認してから発動する。外部サービスへの送信はReactコンポーネントのマウント回数に結び付けず、試合を識別する安定したID(ここでは決定論的なSeed)で多重実行を防止する。

## D-061: サバイバルの行上昇加速幅を緩和

- **日付:** 2026-07-22
- **内容:** サバイバルの共通行上昇設定`survivalRise.accelPerSecondMs`を、毎秒30ms→20msへ変更した。開始間隔5,500ms・技術的floor 50ms・「時間経過により無限に加速する」設計は維持する。floor到達までの目安は約182秒から約273秒となり、終盤までの加速が約1.5倍ゆるやかになる。
- **理由:** 各難易度で速度上昇の幅が大きすぎるという実プレイのフィードバックに対応するため。難易度ごとに別のrise設定を持たせず、全難易度に共通する加速幅だけを調整することで、既存の「難易度差は文章の長さのみ」「誰も無限には生存できない」という両方の方針を保つ。
- **教訓:** 無限加速の有無と、そこへ至る加速曲線の急さは別の調整軸である。終了条件を維持したままプレイ感を緩和したい場合は、floorではなく共通の加速度を先に見直す。

## D-062: サバイバルの行上昇加速幅を再度緩和

- **日付:** 2026-07-22
- **内容:** D-061で30ms/秒から20ms/秒へ緩和した共通の行上昇加速幅を、さらに15ms/秒へ変更した。開始間隔5,500ms・floor 50msは変更しない。floor到達までの目安は約273秒から約363秒になる。
- **理由:** D-061後もブロック落下間隔が縮む幅が大きいという実プレイのフィードバックに対応するため。全難易度で同じ値を使うため、難易度差は引き続きブロック内の文章の長さだけで決まる。
- **教訓:** 速度上昇の体感は加速幅へ直接依存するため、終了条件を変えずにプレイ時間の余裕を増やす調整では、この値を小刻みに見直す。

## D-063: タイピング分析を縮小画面からスクロール型ダッシュボードへ再設計

- **日付:** 2026-07-22
- **内容:** 全画面をビューポート内へ収める`useFitToViewport`を分析画面から外し、読みやすい実寸のまま縦スクロールする画面へ変更した。従来は詳細分析の自然高が約1,575pxある状態で最小倍率55%まで縮小され、さらに末尾の「戻る」ボタンの`autoFocus`によって初期表示が下へ約398pxスクロールしていたため、見出しが画面外に消え、文字・グラフ・キーボードが極端に小さくなっていた。称号/生涯成績と成長グラフをデスクトップでは横並び、今回の指標・ヒートマップ・ペース比較・手指分析を最大1,120px幅のダッシュボードへ整理し、1024px幅では成長グラフを3列、狭い幅では1列に切り替える。分析画面の戻るボタンから`autoFocus`も削除した。
- **同時修正:** ①ミス率0%の指を「特にミスが多め」と表示していたため、ミス率が0より大きい指だけを最弱候補にした。②結果画面に露出していた内部向け文言「登録不要の方針は維持」を、利用者向けの「ニックネームで世界ランキングに登録（任意）」へ変更した。③タイトルの浮遊ブロック演出がタグライン上を横切って可読性を落としていたため、演出範囲をロゴ周辺へ限定し、前景テキストとの重なりを防いだ。
- **検証:** 1280×720で実際にサバイバルをプレイし、ミスを含む打鍵データから結果画面→詳細分析まで通した。修正前の分析は`transform: matrix(0.55, ...)`かつ先頭が画面外だったが、修正後は`transform: none`、初期`scrollY: 0`で見出しから表示されることを確認。1024×768、文字サイズ「特大」、375×812のモバイル専用ランディングでも横方向のはみ出しがないことを確認した。
- **教訓:** ゲーム盤面のような固定比率UIには全体縮小が有効でも、分析・設定・履歴のように情報量が増える画面へ同じ仕組みを適用すると可読性とスクロール位置が破綻する。長文画面では実寸・最大幅・レスポンシブな再配置を優先し、末尾要素への`autoFocus`が初期スクロールを動かさないかも確認する。

## D-064: 公開ポリシー・規約から運営者個人名を削除

- **日付:** 2026-07-22
- **内容:** ユーザーの明示指示により、公開中の`privacy.html`と`terms.html`から運営者の実名を削除し、「TYPE BURST運営チーム」と問い合わせ用メールアドレスの表記へ置き換えた。ランキングは実名・連絡先・アカウントとの紐付けを持たない匿名ニックネーム制であるため、プライバシーポリシーには本名・メールアドレス等の個人を特定できる情報をニックネームへ入力しないよう明記した。
- **理由:** 個人開発者の氏名を一般公開したくないというプライバシー上の要望に対応するため。
- **留意点:** 有料販売・課金・個人を特定できる利用者情報の恒久保存など、将来の機能変更により特定商取引法または個人情報保護法上の表示要件が変わり得る。その時点で法令とサービス仕様を再確認する。

## D-065: 高レベル帯の加速緩和・サバイバル4難易度・入力競合防止

- **日付:** 2026-07-23
- **加速:** 全難易度共通の開始間隔5,500ms、初期加速15ms/秒、技術的下限50msは維持する。30秒のレベル帯ごとに加速率を`1 / sqrt(レベル)`へ緩和し、間隔は短くなり続ける一方で、1レベルあたりの短縮幅を450ms、約318ms、約260ms…と徐々に小さくした。CPU対戦の線形加速には適用しない。
- **難易度:** サバイバルを初級・中級・上級・神級の4段階にした。tier比率は、初級=`micro 80% / short 20%`、中級=`micro 15% / short 65% / standard 20%`（longなし）、上級=`short 10% / standard 65% / long 25%`、神級=`long 100%`とする。上級は旧中級と旧上級の中間より少し上級寄りに位置付けた。ランキングAPIと全画面の難易度表記も神級へ対応した。
- **入力競合:** 「か」と「かき」のように、片方の入力完成形が別の単語の途中と一致する組み合わせを同じ盤面へ出さない。`shi / si / ci`などの代替ローマ字入力を含めてタイピングオートマトン同士の共通遷移を検査し、通常ブロックと妨害ブロックの候補選択で衝突を避ける。候補の開始位置は既存の決定的乱数を使い、リプレイ再現性を維持する。
- **分析画面:** D-063のPC向けダッシュボード化を、添付画像に近い1,900×1,000の表示領域で実プレイから結果・詳細分析まで再検証した。全体縮小なし（`transform: none`）、初期`scrollY: 0`、横幅1,120pxの2カラム、横スクロールなしを確認した。
- **検証:** 型検査、全101テスト、プロダクションビルドを実施する。速度短縮幅の逓減、4難易度の出題tier、生成盤面の入力完成形衝突がないことを自動テストへ追加した。

## D-066: サバイバル加速を一定幅へ戻し、中級を短単語中心へ再調整

- **日付:** 2026-07-23
- **加速:** 高レベル帯だけを緩める傾斜型の加速を撤回し、全レベルで一定の短縮幅へ戻した。開始間隔5,500ms・技術的下限50msは維持し、加速量は直前の15ms/秒から12ms/秒へ少しだけ緩和する。30秒ごとには360msずつ短くなる。
- **中級:** `micro 45% / short 50% / standard 5% / long 0%`に変更した。短単語・短文が95%を占め、`short 10% / standard 65% / long 25%`の上級との差を明確にする。
- **検証:** 一定加速（30秒ごとに360ms）の自動テストへ更新し、型検査・全テスト・プロダクションビルドを実施する。

## D-067: 検索・AI検索向けのブランド統一とクロール基盤

- **日付:** 2026-07-23
- **正規ブランド:** 正式名称を`TYPE BURST`、日本語読みを`タイプバースト（たいぷばーすと）`として、タイトル、説明文、H1、OGP、Web App Manifest、静的紹介ページの表記を統一した。表記ゆれはキーワードの羅列ではなく、自然な本文と`WebSite.alternateName`で関連付ける。
- **技術SEO:** トップページへcanonical、robotsメタ、言語指定、`WebSite`および`SoftwareApplication`のJSON-LDを追加した。初回HTML応答にもゲーム概要と内部リンクを含め、JavaScriptの実行を待たなくても検索クローラーが正式名称・内容・主要導線を取得できるようにした。
- **クロール導線:** `sitemap.xml`を追加して`robots.txt`から通知し、静的な`about.html`へゲーム概要・遊び方・モード・FAQを掲載した。FAQの表示内容と`FAQPage`構造化データは一致させる。IndexNowの検証キーを公開し、本番更新後にトップと紹介ページを通知する。実験的なAIクローラー向け補助情報として`llms.txt`も追加したが、標準規格や順位保証とは扱わない。
- **共有表示:** 正方形のアイコン画像とは別に、ゲームの配色・ブロック・正式名称を使った横長の`og-image-v3.png`を作成し、Open GraphとX Cardをlarge imageへ更新した。
- **運用上の残作業:** 独自ドメインの取得・接続とGoogle Search Console／Bing Webmaster Toolsへの所有権確認・サイトマップ送信はアカウント操作が必要。実施後は新ドメインをcanonical、構造化データ、サイトマップ、OGPの唯一の正規URLへ切り替え、旧Vercel URLから恒久リダイレクトする。

## D-068: `type-burst.com`を唯一の正規ドメインへ移行

- **日付:** 2026-07-23
- **内容:** ユーザーが取得・Vercelへ接続した`https://type-burst.com/`を唯一の正規URLにした。トップ・紹介ページのcanonical、hreflang、OGP、X Card、`WebSite`／`SoftwareApplication`構造化データ、サイトマップ、robots、`llms.txt`、運用ドキュメントを新ドメインへ統一した。
- **転送:** 旧`type-burst-web.vercel.app`の全パスと`www.type-burst.com`の全パスを、対応する`type-burst.com`のパスへ308恒久転送する。旧URLの被リンク評価とクロール信号を正規ドメインへ集約し、重複インデックスを防ぐ。
- **DNS確認:** apexドメインはVercelからHTTPS 200でゲームを配信済み。`www`はDNS解決するが証明書の信頼エラーが残っているため、VercelプロジェクトのDomainsで`www.type-burst.com`も追加し、apexへのRedirectとして証明書発行を完了させる必要がある。
- **検索運用:** 新ドメインでIndexNowを再送信する。Google Search ConsoleとBing Webmaster Toolsは、新ドメインの所有権確認と`https://type-burst.com/sitemap.xml`の送信をユーザー側アカウントで行う。

## D-069: サバイバルの一定加速をわずかに強化

- **日付:** 2026-07-23
- **内容:** D-066で12ms/秒まで緩和した全難易度共通の行上昇加速を13ms/秒へ調整した。傾斜型には戻さず、全レベルで一定幅のまま、30秒ごとの落下間隔短縮を360msから390msへ少しだけ強める。開始間隔5,500ms、技術的下限50ms、難易度間で行上昇速度を変えない方針は維持する。
- **理由:** D-066の調整後は落下周期がわずかに長すぎるという実プレイのフィードバックに対応し、以前の15ms/秒よりは緩やかな中間値を採用した。

## D-070: 毎日遊ぶ理由を作るデイリーチャレンジ

- **日付:** 2026-07-23
- **内容:** 日本時間の日付ごとにSeedが固定される「今日のTYPE BURST」を追加した。全プレイヤーが同じ条件で2分間プレイし、1日3回までをランキング対象、以降は同じステージを無制限で練習できる。通常サバイバルは従来どおりトップアウトまで続き、デイリーだけ`SurvivalGame`の任意制限時間を使用する。
- **継続導線:** 端末内へ連続プレイ日数、自己ベスト、月間カレンダーを保存する。7日継続ごとに最大2枚まで「お休み券」を付与し、1日だけ空いた場合は自動消費して連続記録を保護する。結果画面には今日のベスト更新ボタン、残り挑戦回数、連続記録の結果を表示する。
- **ランキング:** `api/daily-scores.ts`とRedisの日別Sorted Setで、その日の上位、本人順位、参加人数、上位割合、ひとつ上までの必要点を返す。45日で自動失効し、保存済みニックネームがあればランキング対象結果を自動送信する。アカウント登録は要求しない。
- **検証:** 日本時間の日付境界、3回制限、7日継続のお休み券、空白日の保護、2分終了を自動テストへ追加した。型検査、全106テスト、プロダクションビルドを通過した。ローカル画面のブラウザ確認は実行環境のローカルURL接続制限により未実施。

## D-071: デイリーランキングをトップ画面へ常設し、連続記録の説明を明確化

- **日付:** 2026-07-23
- **問題:** D-070の初期UIでは日別ランキングがプレイ終了後にしか現れず、タイトル画面のデイリーカードはカレンダーが大部分を占めていた。そのため、参加人数・自分の順位・上位割合・次順位までの点差をどこで確認できるか分からなかった。また「お休み券」という名称だけでは、取得条件・効果・使用タイミングを理解できなかった。
- **変更:** タイトル画面の「今日のTYPE BURST」内へ本日のランキングを常設した。未挑戦時でも参加人数と上位5名を表示し、「ランキングをもっと見る」で上位100名まで展開できる。自分の記録がある場合は順位、総参加人数、上位割合、次の順位までの点差を同じ場所へ表示する。取得失敗時には再読み込み操作を用意する。デイリーの主操作側は「今日のベスト」「ランキング挑戦の残り回数」「2分間の挑戦」に情報を絞った。
- **連続記録:** 利用者向けの「お休み券」という表現を廃止し、「連続記録キープ（1日休んでも記録が途切れない保護）」へ変更した。7日継続で1回獲得、必要な日に自動使用、という仕組みをカード内へ常時説明する。カレンダーは主情報から外し、「プレイカレンダーを見る」で必要なときだけ展開する。
- **初期表示:** デイリーカード追加後も下段のサバイバルボタンに`autoFocus`が残り、ページ表示時にブラウザがボタンまで自動スクロールしてランキング上部を隠していたため、自動フォーカスを削除した。初期表示は必ずページ最上部から始まり、デイリーの見出しとランキングが先に見える。
- **検証:** 型検査、既存全103テスト、プロダクションビルドを通過した。
